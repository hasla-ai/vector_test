[2장 3강] - 실습: 연립방정식과 행렬 해법

## 필수 1 : 배합 조건을 방정식으로 바꿔 한 번에 풀기

와인 블렌딩 담당자는 원액 두 종류를 섞어 목표 알코올 도수와 목표 산도를 동시에 맞춰야 합니다. "조건 2개, 미지수 2개"인 이 문제는 손으로도 풀 수 있지만, 조건이 늘어나면 계산이 급격히 복잡해집니다. 조건을 행렬 `A`와 벡터 `b`로 정리해 `Ax = b` 한 줄로 표현하고, NumPy로 한 번에 푸는 방법을 익힙니다.

### 문제 1-1 : 조건을 `Ax = b`로 정리하고 풀기

**요구 사항**

1. 다음 조건을 연립방정식으로 정리합니다.
    - 원액 A 1L당 알코올 기여 2, 원액 B 1L당 알코올 기여 1 → 목표 알코올 기여 합계 8
    - 원액 A 1L당 산도 기여 1, 원액 B 1L당 산도 기여 3 → 목표 산도 기여 합계 13
2. 계수 행렬 `A`와 상수항 벡터 `b`를 만들고 shape을 확인합니다.
3. `np.linalg.solve(A, b)`로 해를 구합니다.
4. `A @ x`를 계산해 `b`와 일치하는지 검산합니다.
5. 구한 해를 "원액 A ○L, 원액 B ○L를 섞으면 됩니다" 형식의 문장으로 작성합니다.

- **출력 결과**
    - 계수 행렬 `A`와 상수항 `b`
    - 해 `x`
    - 검산 결과 `A @ x`와 `b`의 일치 여부
    - 배합 결과 해석 문장

```bash
=== [2장-3강] 문제 1-1 출력 결과 ===
--- [1] 계수 행렬 A와 상수항 벡터 b ---
행렬 A:
[[2. 1.]
 [1. 3.]]
행렬 A shape : (2, 2)
벡터 b      : [ 8. 13.]
벡터 b shape : (2,)

--- [2] 연립방정식의 해 x ---
해 벡터 x (x1, x2) : [2.2 3.6]
원액 A 양 (x1)     : 2.20 L
원액 B 양 (x2)     : 3.60 L

--- [3] 검산 결과 (A @ x vs b) ---
복원된 b (A @ x)   : [ 8. 13.]
원래 벡터 b        : [ 8. 13.]
일치 여부          : True

=== [4] 배합 결과 해석 ===
원액 A 2.2L, 원액 B 3.6L를 섞으면 목표 알코올 도수(8)와 산도(13)를 정확히 맞출 수 있습니다.

```

- 각 방정식의 계수를 **행**으로, 미지수를 벡터 `x`로, 상수항을 벡터 `b`로 묶으면 여러 개의 방정식을 하나의 행렬 연산으로 다룰 수 있습니다.
- 해를 구한 뒤에는 항상 `A @ x`를 계산해 `b`와 맞는지 검산합니다. 검산은 코드 오타와 조건 입력 실수를 잡아내는 가장 빠른 방법입니다.

### 문제 1-2 : 역행렬 해법과 `solve` 비교하기

**요구 사항**

1. `np.linalg.inv(A) @ b`로 해를 구해 `solve` 결과와 비교합니다.
2. 두 결과의 차이를 노름으로 확인합니다.
3. 조건을 살짝 바꿔 거의 종속에 가까운 행렬 `A_ill = [[2, 1], [2.000001, 1.0000004]]`와 상수항 `b_ill = [8, 8.000002]`를 만들고 조건수(`np.linalg.cond`)를 계산합니다. (2행이 1행의 정확한 배수가 되면 특이행렬이 되어 이후 단계를 진행할 수 없으니 숫자를 그대로 사용하세요.)
4. `A_ill`에 대해 두 방식으로 해를 구해 결과가 얼마나 벌어지는지 확인하고, 이어서 `b_ill`의 둘째 원소를 `0.0000001`만 바꿔 다시 풀어 해가 얼마나 움직이는지 보여 줍니다.
5. 실무에서 역행렬을 직접 구하기보다 `solve`를 권장하는 이유를 한 문장으로 작성합니다.

- **출력 결과**
    - 두 방식의 해와 차이
    - `A_ill`의 조건수
    - `A_ill`에서 두 방식의 해 차이
    - `solve` 권장 이유

```bash
=== [2장-3강] 문제 1-2 출력 결과 ===
--- [1] 표준 행렬 A에서 inv vs solve 비교 ---
• np.linalg.inv(A) @ b 해 : [2.2 3.6]
• np.linalg.solve(A, b) 해  : [2.2 3.6]
• 두 해의 차이 (L2 Norm)    : 4.440892e-16

--- [2] 병태 행렬 A_ill에서 조건수 및 두 방식 차이 ---
• A_ill 조건수 (Condition Number) : 50,000,024.16
• A_ill에서 inv 해                 : [-6.00000001 20.00000001]
• A_ill에서 solve 해               : [-6. 20.]
• A_ill에서 두 해의 차이 (L2 Norm)  : 1.643830e-08

--- [3] 미세한 입력을 변화(0.0000001)시켰을 때 해의 변동성 ---
• 변경 전 solve 해 (b_ill)           : [-6. 20.]
• 미세 변경 후 solve 해 (b_perturbed) : [-5.5        19.00000001]
• 해의 위치 이동 거리 (L2 Norm)      : 1.118034

=== [4] 실무에서 inv보다 solve를 권장하는 이유 ===
역행렬을 직접 구하는 연산($O(n^3)$)은 메모리 소모가 크고 부동소수점 오차 누적에 매우 취약한 반면, solve 함수는 LU 분해 기반의 고성능 수치 알고리즘을 사용하여 계산 효율성과 연산 안정성이 훨씬 뛰어나기 때문입니다.
```

- 조건이 좋은 행렬에서는 두 방식의 결과가 사실상 같습니다.
- `A_ill`은 `cond ≈ 5.0e+07`로 조건이 매우 나쁩니다. 여기서 역행렬 방식과 `solve`의 해는 노름 기준 `1e-8` 수준으로 벌어집니다. 숫자만 보면 작지만, 배정밀도 한계(`1e-16`)에 비하면 **반올림 오차가 여덟 자릿수만큼 증폭**된 결과입니다.
- 더 직관적인 위험은 `b`에 대한 민감도입니다. `b`의 둘째 원소를 `0.0000001`만 바꿔도 해가 `[-6, 20]`에서 `[-5.5, 19]`로 움직입니다. 입력 오차는 상대적으로 `1e-8`인데 해가 몇 % 단위로 틀어지는 것이 악조건의 정의입니다.
- 만약 2행을 1행의 **정확한** 배수(예: `[2.000001, 1.0000005]`)로 잡으면 행렬이 아예 특이행렬이 되어 `solve`와 `inv` 모두 `LinAlgError`로 중단됩니다. "악조건"과 "특이"는 다른 상황이니 구별해야 합니다.
- `np.linalg.solve`는 역행렬을 명시적으로 만들지 않고 바로 해를 구하므로 계산량이 적고 수치적으로 더 안정적입니다.

## 필수 2 : 답이 하나가 아닐 수도 있다

블렌딩 조건을 바꿨더니 어떤 배합으로도 목표를 맞출 수 없거나, 반대로 여러 배합이 모두 조건을 만족하는 상황이 생깁니다. `solve`는 이런 경우 오류를 내거나 잘못된 답을 줍니다. 계산 전에 rank를 비교해 해가 어떤 종류인지 먼저 판단하는 습관을 들입니다.

### 문제 2-1 : rank 비교로 해의 종류 판별하기

**요구 사항**

1. 다음 세 가지 시스템을 정의합니다.
    - **유일해**: `A1 = [[2, 1], [1, 3]]`, `b1 = [8, 13]`
    - **무한해**: `A2 = [[2, 1], [4, 2]]`, `b2 = [8, 16]` (두 번째 식이 첫 식의 2배)
    - **불능**: `A3 = [[2, 1], [4, 2]]`, `b3 = [8, 20]` (좌변은 배수인데 우변은 아님)
2. 각 시스템에 대해 `rank(A)`, `rank([A|b])`, 변수 수를 계산하고 이론의 판정 기준과 비교합니다.
3. 세 결과를 표로 정리합니다.
4. 각 시스템에 `np.linalg.solve`를 시도해 어떤 결과·오류가 나는지 확인합니다.
5. 세 경우를 두 직선의 위치 관계(한 점에서 만남 / 완전히 겹침 / 평행)로 해석해 작성합니다.

- **출력 결과**
    - 세 시스템의 rank 비교 표
    - 각 시스템의 판정 결과
    - `solve` 시도 결과와 오류 메시지
    - 기하학적 해석

```bash
=== [2장-3강] 문제 2-1 출력 결과 ===
--- [1] 세 시스템의 Rank 비교 및 판정 표 ---
        시스템 종류  rank(A)  rank([A|b])  변수 수(n)   이론적 판정
유일해 (System 1)        2            2        2      유일해
무한해 (System 2)        1            1        2      무한해
 불능 (System 3)        1            2        2 해 없음(불능)

--- [2] np.linalg.solve 시도 결과 및 오류 메시지 ---
• 유일해 (System 1)     : 성공: x = [2.2 3.6]
• 무한해 (System 2)     : LinAlgError 발생 (Singular matrix)
• 불능 (System 3)      : LinAlgError 발생 (Singular matrix)

=== [3] 기하학적 해석 (2차원 평면상 두 직선의 위치 관계) ===
1) 유일해 (System 1): 두 직선의 기울기가 서로 달라 평면상에서 정확히 한 점에서 만납니다.
2) 무한해 (System 2): 두 직선의 기울기와 y절편이 일치하여 완벽하게 겹치므로(동일 직선), 직선 위의 모든 점이 해가 됩니다.
3) 불능   (System 3): 두 직선의 기울기는 같으나 y절편이 달라 서로 평행하여 영원히 만나지 않으므로 해가 존재하지 않습니다.
```

- 무한해와 불능 케이스는 `A`가 특이행렬이라 `solve`가 실패합니다. **오류 메시지만으로는 두 경우를 구분할 수 없으므로**, rank를 먼저 비교해야 원인을 정확히 알 수 있습니다.
- 무한해는 조건이 중복된 상황(같은 정보가 두 번 들어옴), 불능은 조건이 서로 모순인 상황입니다. 대응 방법이 완전히 다르므로 구분이 중요합니다.

### 문제 3-1 : 최소제곱해와 정규방정식 비교하기

**요구 사항**

1. Wine Quality 데이터를 표준화하고 앞에 1로 채운 절편 컬럼을 붙여 설계행렬 `Xd`를 만듭니다.
2. `Xd`의 shape을 확인해 방정식 수와 미지수 수를 비교하고, `describe_solution()`으로 `Xd x = y`의 해 종류를 판정합니다.
3. `np.linalg.lstsq`로 최소제곱해 `coef_lstsq`를 구하고 RMSE를 계산합니다.
4. 정규방정식 `XᵀXβ = Xᵀy`를 `np.linalg.solve`로 풀어 `coef_normal`을 구합니다.
5. 두 계수의 차이와 `XᵀX`의 조건수를 확인합니다.
6. 정확한 해가 없는 문제에서도 최소제곱해는 항상 구할 수 있는 이유, 그리고 실무에서 정규방정식보다 `lstsq`를 권장하는 이유를 2~3문장으로 작성합니다.

- **출력 결과**
    - 설계행렬 shape과 해 종류 판정
    - 최소제곱해의 rank와 RMSE
    - 정규방정식 계수와 최소제곱 계수의 차이
    - `XᵀX` 조건수
    - 해석 서술

```bash

```

- 방정식 수가 미지수 수보다 훨씬 많으면 `Xd x = y`를 정확히 만족하는 해는 거의 존재하지 않습니다(불능). 대신 오차 벡터 `Xd x − y`의 길이를 최소로 만드는 **최소제곱해**를 구합니다.
- 정규방정식 `XᵀXβ = Xᵀy`는 항상 풀 수 있는 형태로 바꿔 주기 때문에, 해가 없던 문제도 하나의 답을 얻을 수 있습니다.
- 다만 `XᵀX`를 만드는 과정에서 조건수가 원래 행렬의 제곱 수준으로 커져 수치 오차가 누적될 수 있습니다. 그래서 실무에서는 `XᵀX`를 명시적으로 만들지 않는 `lstsq`(내부적으로 SVD 사용)나 QR 분해를 사용합니다. 이 내용은 3장 3강에서 이어집니다.