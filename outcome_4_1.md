
## ** [4장 1강] - 실습: SVD 구조와 저랭크 근사 ** ##

## 필수 1 : 정사각형이 아닌 행렬도 분해하기

데이터 플랫폼팀은 고객 60명 × 상품 40개 구매량 행렬을 다뤄야 합니다. 3장에서 배운 고유분해는 정사각행렬에만 쓸 수 있어 이 행렬에는 적용할 수 없습니다. 어떤 모양의 행렬이든 분해할 수 있는 SVD를 적용해, 행렬을 "고객 쪽 축 × 중요도 × 상품 쪽 축"으로 나눠 봅니다.

### 문제 1-1 : SVD를 적용하고 각 행렬의 shape 해석하기

**요구 사항**

1. `np.linalg.svd(M, full_matrices=False)`로 `U`, `S`, `Vt`를 구합니다.
2. `M`, `U`, `S`, `Vt`의 shape을 출력하고 각각이 무엇을 담고 있는지 설명합니다.
3. `full_matrices=True`로도 분해해 shape이 어떻게 달라지는지 비교합니다.
4. `S`가 내림차순으로 정렬되어 있는지 확인하고 상위 5개 특이값을 출력합니다.
5. `S`가 1차원 배열로 반환되는 이유를 한 문장으로 설명합니다.

- **출력 결과**
    - 축소형/전체형 SVD의 shape 비교
    - 상위 5개 특이값과 내림차순 여부
    - 각 행렬의 역할 설명

```bash
고객-상품 행렬 M Shape: (60, 40)

==================================================
[필수 1] 문제 1-1: SVD 적용 및 shape 해석
==================================================
축소형 SVD -> U: (60, 40), S: (40,), Vt: (40, 40)
- U: (60, 40) -> 고객 공간의 직교 잠재 특성 벡터 (좌특이벡터)
- S: (40,) -> 각 잠재 축의 중요도를 나타내는 특이값 배열
- Vt: (40, 40) -> 상품 공간의 직교 잠재 특성 벡터 (우특이벡터)
전체형 SVD -> Uf: (60, 60), Sf: (40,), Vtf: (40, 40)
특이값 내림차순 정렬 여부: True
상위 5개 특이값: [14845.40674128 13351.76017407 12540.30758312 11638.47650906
 10191.47441758]

[설명 1-1-5] S는 대각 성분 외의 모든 값이 0인 대각행렬이므로 메모리 효율성을 위해 대각 원소만 1차원 배열로 반환합니다.
```

- 정사각행렬이 아니어도, 대칭행렬이 아니어도 SVD는 **항상 존재**합니다. 고유분해와 가장 큰 차이입니다.
- `full_matrices=True`는 `U`를 (60, 60)까지 채우지만, 뒤쪽 열은 복원에 쓰이지 않습니다. 실무에서는 계산량이 적은 축소형을 주로 사용합니다.
- `S`는 대각 원소만 의미가 있으므로 NumPy가 **1차원 배열로 반환**합니다. 행렬로 쓰려면 `np.diag(S)`로 바꿔야 합니다.

### 문제 1-2 : 직교성 확인과 원본 복원하기

**요구 사항**

1. `U.T @ U`와 `Vt @ Vt.T`가 각각 단위행렬인지 확인합니다.
2. `np.diag(S)`로 Σ를 만들고 `U @ Σ @ Vt`를 계산합니다. 이때 **복원은 반드시 `full_matrices=False`로 얻은 `U`, `S`, `Vt`로 합니다.** 문제 1-1의 전체형 결과(`Uf`, `Sf`, `Vtf`)를 쓰면 `Uf`는 (60, 60), `np.diag(Sf)`는 (40, 40)이 되어 행렬곱 shape이 맞지 않아 오류가 납니다.
3. 복원 결과와 원본 `M`의 차이(프로베니우스 노름)를 계산합니다.
4. 상대 오차(`복원 오차 / 원본 노름`)도 함께 계산합니다.
5. 모든 특이값을 사용했을 때 오차가 사실상 0인 이유를 한 문장으로 설명합니다.

- **출력 결과**
    - `UᵀU`, `VVᵀ` 검증 결과
    - 복원 오차와 상대 오차
    - 완전 복원에 대한 설명

```bash
==================================================
[필수 1] 문제 1-2: 직교성 확인과 원본 복원
==================================================
U^T @ U 가 단위행렬인가? True
Vt @ Vt^T 가 단위행렬인가? True
복원 오차(Frobenius Norm): 6.819560466610e-11
상대 오차: 1.868279325958e-15

[설명 1-2-5] 정규직교 기저 U, V와 모든 특이값을 사용한 UΣVᵀ 계산은 수학적으로 원본 행렬과 완전 동일하므로 남은 오차는 부동소수점 연산 오차뿐입니다.
```

- `U`와 `V`의 열들은 서로 직교하고 길이가 1인 정규직교 벡터입니다. 3장 3강의 직교행렬 성질이 그대로 적용됩니다.
- 모든 특이값을 사용하면 `UΣVᵀ`는 원본과 **수학적으로 완전히 같습니다.** 남는 오차는 부동소수점 반올림 수준(1e-12 이하)뿐입니다.
- 상대 오차로 확인하면 데이터 크기에 상관없이 복원 품질을 판단할 수 있습니다.

## 필수 2 : 특이값은 어디에서 오는가

특이값이 그냥 주어지는 숫자가 아니라 `AᵀA`의 고유값에서 나온다는 점을 확인하면, 3장의 고유분해와 4장의 SVD가 하나의 이야기로 연결됩니다. 또한 이 행렬에 고유분해를 직접 시도해 보면 왜 SVD가 필요한지 분명해집니다.

### 문제 2-1 : 특이값과 `AᵀA` 고유값의 관계 확인하기

**요구 사항**

1. `MᵀM`을 계산하고 대칭행렬인지 확인합니다.
2. `np.linalg.eigvalsh(MᵀM)`로 고유값을 구하고 내림차순으로 정렬합니다.
3. 고유값에 제곱근을 취한 값과 특이값 `S`를 비교합니다.
4. 상위 5개에 대해 두 값을 표로 나란히 출력하고 최대 차이를 계산합니다.
5. 음수 반올림 오차가 생길 수 있으므로 제곱근을 취하기 전 `np.clip(..., 0, None)`을 적용하는 이유를 설명합니다.
6. 실데이터 `M`은 `MᵀM`의 최소 고유값도 충분히 큰 양수여서, clip이 있으나 없으나 결과가 같습니다. clip이 진짜로 필요해지는 상황을 직접 만들어 봅니다. **랭크가 부족한 행렬** `R = C @ P`(`C`는 60×5, `P`는 5×40이므로 `rank(R) ≤ 5`)를 만들고, `RᵀR`의 고유값을 구해 **음수 고유값이 몇 개 나오는지** 세어 봅니다.
7. 그 고유값에 clip 없이 `np.sqrt`를 바로 적용해 `nan`이 몇 개 생기는지 확인하고, clip을 적용한 결과와 비교합니다.

- **출력 결과**
    - `MᵀM`의 대칭 여부
    - 고유값 제곱근과 특이값 비교 표
    - 두 값의 최대 차이
    - 랭크 결손 행렬 `R`의 rank와 음수 고유값 개수
    - clip 적용 전후 `nan` 개수 비교

``` bash

```

- 특이값은 `AᵀA`(또는 `AAᵀ`) 고유값의 **제곱근**입니다. `AᵀA`는 항상 대칭이고 고유값이 0 이상이므로, 특이값은 언제나 0 이상의 실수가 됩니다.
- 수치 계산 과정에서 0에 가까운 고유값이 아주 작은 음수로 나올 수 있습니다. 그대로 제곱근을 취하면 `nan`이 되므로 `np.clip(..., 0, None)`으로 0 이하 값을 잘라 냅니다.
- **이번 실데이터에서는 clip이 없어도 결과가 같습니다.** `MᵀM`의 최소 고유값이 이미 충분히 큰 양수이기 때문입니다. clip의 필요성은 이 행렬만 봐서는 체감되지 않습니다.
- 그래서 **랭크가 부족한 행렬** `R = C @ P`를 따로 만들어 확인합니다. `R`은 5개 잠재방향으로만 만들어졌으므로 이론적으로 0이어야 하는 고유값이 35개 있고, 그중 **17개가 실제로 아주 작은 음수**(예: -8.1e-13)로 계산됩니다. 이때 clip 없이 `np.sqrt`를 쓰면 `nan`이 17개 생기고 NumPy가 `RuntimeWarning: invalid value encountered in sqrt`를 냅니다.
- 추천행렬처럼 **잠재 요인 수가 상품 수보다 훨씬 적은** 데이터는 항상 이 상황에 해당합니다. 그래서 고유값에 제곱근을 취하는 코드에는 데이터에 상관없이 clip을 기본으로 붙여 두는 것이 안전합니다.

### 문제 2-2 : 고유분해와 SVD의 적용 범위 비교하기

<aside>

**요구 사항**

1. 직사각행렬 `M`에 `np.linalg.eig`를 직접 시도하고 어떤 오류가 나는지 확인합니다.
2. 같은 `M`에 SVD를 적용해 정상적으로 분해되는지 확인합니다.
3. 정사각이지만 대각화가 불가능한 `B = [[1, 1], [0, 1]]`에 대해 SVD를 적용해 특이값을 확인합니다.
4. 고유분해와 SVD의 차이를 적용 대상 / 결과 벡터의 직교성 / 수치 안정성 관점에서 표로 정리합니다.
5. 어떤 상황에서 SVD를 선택해야 하는지 한 문장으로 작성합니다.
</aside>

- **출력 결과**
    - 직사각행렬에 고유분해 시도 시 오류 메시지
    - 같은 행렬의 SVD 성공 결과
    - `B`의 특이값
    - 두 분해 비교 표

```
bash

```

- 고유분해는 정사각행렬이 아니면 애초에 정의되지 않습니다. 3장 2강에서 봤던 대각화 불가 행렬 `B`도 SVD로는 문제없이 분해됩니다.
- 실제 데이터는 행 수와 열 수가 다른 경우가 대부분이므로, 차원 축소·행렬 근사에서는 SVD를 기본 도구로 사용합니다.

## 심화 1 : 상위 몇 개만 남겨도 원본을 설명할 수 있을까

<aside>

고객-상품 행렬을 그대로 저장하고 다루면 용량과 계산 비용이 큽니다. 특이값이 내림차순이라는 점을 이용해 상위 k개만 남기면 저장 공간을 크게 줄일 수 있습니다. 어느 정도까지 줄여도 원본을 충분히 설명하는지 오차와 보존 에너지로 확인합니다.

</aside>

### 문제 3-1 : 저랭크 근사의 오차와 정보 보존량 계산하기

<aside>

**요구 사항**

1. `k = 1, 2, 5, 10, 20`에 대해 `M_k = U[:, :k] @ diag(S[:k]) @ Vt[:k, :]`를 계산합니다.
2. 각 `k`에 대해 다음을 계산해 표로 정리합니다.
    - 복원 오차 `||M - M_k||`
    - 상대 오차 `||M - M_k|| / ||M||`
    - 보존 에너지 `sum(S[:k]²) / sum(S²)`
    - 저장에 필요한 원소 수 `k * (행 + 열 + 1)`와 원본 원소 수 대비 비율
3. `k`에 따른 보존 에너지 곡선을 그립니다.
4. 보존 에너지가 90%를 넘는 최소 `k`를 찾습니다.
5. `M_k`의 rank가 실제로 `k`인지 확인합니다.
6. 어느 정도의 정보 손실까지 허용할지 결정할 때 고려할 점을 2~3문장으로 작성합니다.

- **출력 결과**
    - k별 오차·보존 에너지·저장 비용 표
    - 보존 에너지 곡선
    - 90% 기준 최소 k와 해당 근사 행렬의 rank
    - 정보 손실 허용 기준에 대한 서술

```bash

```

- `k`가 커질수록 오차가 줄고 보존 에너지가 1에 가까워집니다. 특이값이 내림차순이므로 **앞쪽 몇 개가 정보의 대부분**을 담고 있습니다.
- 저장 원소 수는 `k(m + n + 1)`로, 원본 `m × n`보다 훨씬 작습니다. 이것이 이미지 압축·추천 시스템에서 저랭크 근사를 쓰는 이유입니다.
- `M_k`의 rank는 정확히 `k`입니다. 저랭크 근사는 말 그대로 **rank를 낮춘 행렬로 원본을 대신하는 것**입니다.
- **참고 기준값:** UCI 실데이터로 진행하면 보존 에너지 90%에 도달하는 최소 `k`가 40개 중 12개 전후, 대체 데이터(`synthetic_retail()`)로 진행하면 6개 전후로 나옵니다. 구체적인 숫자는 다르지만 **열 개수의 1/3 이하만으로 90%를 설명한다**는 결론은 동일하게 나와야 합니다. 만일 `k`가 열 개수에 가깝게 나온다면 데이터에 저랭크 구조가 없는 경우이므로, 준비 코드가 제대로 실행되었는지 확인하세요.
- 허용 손실 기준은 목적에 따라 다릅니다. 시각화나 탐색이 목적이면 80~90%로도 충분하지만, 예측 성능이 중요한 경우에는 k를 바꿔 가며 실제 지표(정확도, RMSE 등)를 측정해 결정해야 합니다.