
## 필수 1 : 행렬을 "축 + 배율"로 분해하기

행렬 변환을 여러 번 반복 적용해야 하는 상황(예: 상태 전이를 100번 반복)에서는 행렬 곱을 100번 하는 대신, 행렬을 축과 배율로 분해해 두면 계산이 훨씬 간단해집니다. 이 분해가 **행렬 대각화** `A = PDP⁻¹`이며, 다음 단계의 PCA도 정확히 같은 구조 위에서 동작합니다.

### 문제 1-1 : `A = PDP⁻¹` 구성하고 복원하기

**요구 사항**

1. `A = [[4, 2], [1, 3]]`을 고유분해해 고유값과 고유벡터를 구합니다.
2. 고유벡터를 열로 쌓은 행렬 `P`와, 고유값을 대각선에 배치한 대각행렬 `D`를 만듭니다.
3. `P @ D @ inv(P)`를 계산해 원본 `A`와 같은지 `np.allclose`로 확인합니다.
4. `P`의 행렬식을 계산해 역행렬이 존재하는지 확인합니다.
5. 대각화가 무엇을 무엇으로 바꾸는 것인지 한 문장으로 설명합니다.

- **출력 결과**
    - `P`와 `D` 행렬
    - `PDP⁻¹` 복원 결과와 일치 여부
    - `det(P)` 값

```bash
=== [문제 1-1] A = PDP⁻¹ 구성하고 복원하기 ===             
1. P 및 D 행렬                                      
• P (고유벡터 행렬):                                      
 [[ 0.89442719 -0.70710678]                           
 [ 0.4472136   0.70710678]]
• D (고유값 대각행렬):
 [[5. 0.]
 [0. 2.]]

2. PDP⁻¹ 복원 검증
• PDP⁻¹ 계산 결과:
 [[4. 2.]
 [1. 3.]]
• 원본 A와 일치 여부: True

3. det(P) 값: 0.948683 (≠ 0 이므로 역행렬 P⁻¹ 존재 함)

4. 대각화의 의미
대각화는 행렬 A를 '좌표축을 바꾸고(P⁻¹) -> 축별로 배율만 곱하고(D) -> 원래 좌표계로 되돌리는(P)' 세 단계로 분해하는 것입니다.
```

- 대각화는 행렬 `A`를 **"좌표축을 바꾸고(P⁻¹) → 축별로 배율만 곱하고(D) → 원래 좌표계로 되돌리는(P)"** 세 단계로 분해하는 것입니다.
- `P`의 열은 고유벡터, `D`의 대각 원소는 대응하는 고유값입니다. `det(P)`가 0이 아니어야 `P⁻¹`가 존재하고 대각화가 성립합니다.

### 문제 1-2 : 대각화로 거듭제곱 계산하고, 불가능한 경우 확인하기

**요구 사항**

1. `A @ A`와 `P @ D**2 @ inv(P)`를 계산해 같은지 확인합니다. (`D`는 대각행렬이므로 대각 원소만 제곱하면 됨)
2. `A`의 10제곱을 두 방식으로 계산해 같은지 확인합니다.
3. `B = [[1, 1], [0, 1]]`을 고유분해하고, 고유값과 고유벡터의 개수·독립성을 확인합니다.
4. 복원을 시도하기 **전에** `np.linalg.matrix_rank`와 `np.linalg.cond`로 고유벡터 행렬의 가역 여부를 사전 판별합니다. 그런 다음 `P @ D @ inv(P)`를 계산해 원본 `B`와 비교합니다.
    - 주의: NumPy는 이 경우 **에러를 내지 않습니다.** `try/except np.linalg.LinAlgError`로 기다려도 예외가 잡히지 않고, 대신 원본과 전혀 다른 값(단위행렬)이 조용히 나옵니다. 그래서 예외를 기다리는 방식이 아니라 `rank`·`cond`로 **사전에** 판별해야 합니다.
5. 대칭행렬(예: 공분산행렬)이 항상 대각화 가능한 이유를 한 문장으로 작성합니다.

- **출력 결과**
    - 두 방식의 `A²`, `A¹⁰` 일치 여부
    - `B`의 고유값과 고유벡터, `rank(P_B)`
    - `B` 고유벡터 행렬의 `cond` 값과 대각화 가능 여부 사전 판정 결과
    - `B` 복원 결과와 원본 `B`의 비교 (에러가 아니라 **틀린 값**이 나오는 것)
    - 대칭행렬의 대각화 보장에 대한 설명

```bash
=== [문제 1-2] 대각화로 거듭제곱 계산하고, 불가능한 경우 확인하기 ===
1. 거듭제곱 검증 결과
• A²  일치 여부: True                          
• A¹⁰ 일치 여부: True                                      
                                                 
2. 결함 행렬 B의 고유분해 및 사전 판정                  
• B의 고유값: [1. 1.]
• B의 고유벡터 행렬 P_B:
 [[ 1.00000000e+00 -1.00000000e+00]
 [ 0.00000000e+00  2.22044605e-16]]
• rank(P_B) : 1 (2차원 행렬에서 rank 2 미만이므로 결함 행렬)
• cond(P_B) : 9.01e+15 (매우 큰 조건수로 인해 수치적 불안정)

3. B의 PDP⁻¹ 복원 시도 결과
• P_B @ D_B @ inv(P_B) 복원 값:
 [[1. 0.]
 [0. 1.]]
• 원본 B와 일치 여부: False
  (NumPy 예외 없이 계산되지만 원본 B가 아닌 단위행렬로 '틀린 값' 출력)

4. 대칭행렬의 대각화 보장
대칭행렬은 항상 n개의 직교하는 고유벡터를 가지므로 P가 항상 가역(P⁻¹ = Pᵀ)이 되어 대각화가 문제없이 보장됩니다.
```

- 대각행렬은 거듭제곱이 **대각 원소의 거듭제곱**이므로, `A¹⁰`을 행렬 곱 10번 대신 `P D¹⁰ P⁻¹` 한 번으로 계산할 수 있습니다.
- `B`는 고유값이 1로 중복되고 독립인 고유벡터가 1개뿐이라 `P`의 rank가 2가 되지 않습니다. 이런 행렬은 대각화가 불가능합니다.
- 여기서 가장 주의해야 할 지점은 **NumPy가 에러를 내준다고 기대하면 안 된다**는 점입니다. `vecs_B`의 둘째 열에 `2.22e-16` 같은 미세한 값이 남아 있어서 `np.linalg.inv`가 `LinAlgError` 없이 그대로 통과하고, `P D P⁻¹`는 원본 `B`가 아니라 **단위행렬**을 돌려줍니다. 수치적으로는 "조용한 오답"이 가장 위험합니다.
- 그래서 현장에서는 `try/except`로 예외를 기다리는 대신, `matrix_rank`로 독립인 고유벡터 개수를 세거나 `cond`로 조건수를 보아 **복원 전에 대각화 가능 여부를 판별**하고, 복원 후에는 반드시 `np.allclose`로 원본과 대조해야 합니다.
- 반면 대칭행렬은 **항상 n개의 직교하는 고유벡터**를 가지므로 `P`가 항상 가역이고, 심지어 `P⁻¹ = Pᵀ`가 됩니다. 공분산행렬이 대칭이라 PCA에서 대각화가 언제나 안전하게 보장됩니다.

## 필수 2 : 품질 데이터의 핵심 축을 직접 찾아내기

와인 품질 분석팀은 이화학 측정 항목이 10개가 넘어 산점도로 한눈에 보기 어렵습니다. 서로 상관이 높은 항목들을 묶어 **분산이 가장 큰 방향부터** 새 축으로 잡으면, 적은 축으로도 데이터 구조를 대부분 설명할 수 있습니다. 이 절차가 PCA이며, 앞에서 다룬 대각화를 공분산행렬에 적용한 것입니다.

### 문제 2-1 : PCA를 NumPy로 직접 구현하기

**요구 사항**

1. 표준화된 `Xs`로 공분산행렬을 계산합니다.
2. `np.linalg.eigh`로 고유분해하고 고유값을 **내림차순**으로 정렬합니다. (고유벡터 열 순서도 함께)
3. 상위 2개 고유벡터로 데이터를 투영해 `Z_manual`을 만듭니다.
4. `Z_manual`의 shape과 상위 2개 주성분의 설명분산비(`고유값 / 고유값 합`)를 계산합니다.
5. 투영 결과의 두 축이 서로 상관이 거의 없는지(상관계수 ≈ 0) 확인합니다.

- **출력 결과**
    - 투영 결과 shape
    - 상위 2개 설명분산비와 누적값
    - 두 주성분 축의 상관계수

```bash

```

- PCA는 **표준화 → 공분산행렬 → 고유분해 → 상위 k개 고유벡터로 투영**의 네 단계로 끝납니다. 각 단계가 앞에서 배운 연산의 조합입니다.
- 설명분산비는 `해당 고유값 / 전체 고유값 합`으로, 그 주성분이 전체 정보 중 몇 %를 담고 있는지를 나타냅니다.
- 주성분 축끼리는 직교하므로 투영 결과의 상관계수가 0에 가깝습니다. 즉 새 축들은 서로 중복 정보를 담지 않습니다.

### 문제 2-2 : sklearn PCA와 결과 비교하기

<aside>

**요구 사항**

1. `PCA(n_components=2)`를 `Xs`에 적용해 `Z_sklearn`을 구합니다.
2. `pca.explained_variance_ratio_`를 직접 계산한 설명분산비와 비교합니다.
3. `pca.components_`와 직접 구한 고유벡터를 **절댓값 기준**으로 비교합니다.
4. `Z_manual`과 `Z_sklearn`도 절댓값 기준으로 일치하는지 확인합니다.
5. `PCA()`로 **전체 주성분**을 구해, 성분별로 sklearn과 직접 구한 고유벡터의 부호가 같은지 다른지 표로 정리합니다.
    - 주의: 부호 반전은 **항상 일어나는 것이 아니라 성분에 따라 다릅니다.** PC1·PC2는 부호가 그대로 같게 나올 수도 있으므로, 상위 두 개만 보고 "부호 반전은 없다"고 결론 내리지 말고 뒤쪽 성분까지 함께 확인하세요.
6. PCA 결과를 2차원 산점도로 그립니다.
7. 부호가 뒤집혀 나올 수 있는 이유를 한 문장으로 설명합니다.
</aside>

- **출력 결과**
    - sklearn 설명분산비와 직접 계산값 비교
    - 주성분 방향·투영 결과의 일치 여부
    - 성분별 부호 일치/반전 표 (전체 주성분)
    - PCA 2D 산점도

```bash

```

- 직접 구현 결과와 sklearn 결과가 절댓값 기준으로 일치합니다. 라이브러리가 내부에서 무엇을 하는지 확인했으므로 결과를 신뢰할 수 있습니다.
- 고유벡터 `v`와 `-v`는 **같은 축을 반대 방향으로 가리킬 뿐** 수학적으로 동등합니다. 그래서 구현체마다 부호가 다를 수 있고, 비교할 때 절댓값을 사용합니다.
- 단, 부호 반전이 **모든 성분에서 일어나지는 않습니다.** 상위 주성분(PC1·PC2)은 부호가 그대로 같게 나오고 뒤쪽 성분 몇 개에서만 반전되는 경우가 흔합니다. 상위 두 개만 보고 "부호는 원래 같다"고 오해하기 쉬우니, 위 표처럼 전체 성분을 한꺼번에 확인하세요.
- 그리고 이번 환경에서 부호가 우연히 같게 나왔다는 것이 "절댓값 비교를 생략해도 된다"는 뜻은 아닙니다. LAPACK 버전·운영체제에 따라 바뀔 수 있으므로 비교할 때는 **항상 절댓값 기준**을 쓰십시오.
- 산점도에서 품질 점수별로 구간이 나뉘는 경향이 보인다면, 2개 축만으로도 품질과 관련된 구조가 일부 드러난 것입니다.

## 심화 1 : 몇 개의 축까지 남길 것인가

<aside>

차원을 2개로 줄이면 시각화는 편하지만 정보 손실이 큽니다. 반대로 너무 많이 남기면 축소의 의미가 없습니다. 실무에서는 **누적 설명분산비**를 기준으로 목표치(예: 90%)를 정하고 필요한 주성분 개수를 결정합니다.

</aside>

### 문제 3-1 : 누적 설명분산비로 차원 수 결정하기

<aside>

**요구 사항**

1. `PCA()`를 전체 주성분에 대해 적합시키고 `explained_variance_ratio_`를 구합니다.
2. 누적 설명분산비를 계산해 꺾은선 그래프로 그리고, 90% 기준선을 함께 표시합니다.
3. 누적 설명분산비가 각각 80% / 90% / 95%를 넘는 최소 주성분 개수를 구합니다.
4. 원본 특성 수 대비 축소율을 계산해 표로 정리합니다.
5. 표준화를 하지 않은 원본 데이터로 같은 계산을 해 첫 주성분의 설명분산비를 비교합니다.
6. 기준치를 정할 때 고려해야 할 점을 2~3문장으로 작성합니다.
</aside>

- **출력 결과**
    - 누적 설명분산비 그래프
    - 기준별 필요한 주성분 개수와 축소율 표
    - 표준화 여부에 따른 PC1 설명분산비 비교
    - 기준 선택에 대한 서술

```bash

```

- 누적 설명분산비 곡선이 완만해지는 지점 이후로는 주성분을 더 추가해도 얻는 정보가 적습니다. 기준선을 그어 필요한 개수를 정하면 근거 있는 결정이 됩니다.
- 표준화를 생략하면 값의 범위가 큰 항목 하나가 분산을 독점해 **PC1의 설명분산비가 비정상적으로 높게** 나옵니다. 정보를 잘 압축한 것이 아니라 특정 항목만 남긴 것이므로 반드시 표준화를 먼저 적용해야 합니다.
- 기준치는 목적에 따라 달라집니다. 시각화가 목적이면 2~3개로 고정하고, 모델 입력이 목적이면 90~95% 수준을 잡되 계산 비용과 성능을 함께 비교해 결정합니다.
