[5장 2강] - 실습: reshape·transpose·broadcasting

## 필수 1 : 센서 기록을 모델이 원하는 형태로 바꾸기

설비 예지보전 모델은 "6개 시점을 한 구간으로 묶은 배치" 형태의 입력을 요구합니다. 지금 데이터는 `(레코드 수, 센서 수)`인 2차원 표라서 그대로 넣을 수 없습니다. 원소는 그대로 두고 축 구조만 바꾸는 `reshape`와, 축 순서를 바꾸는 `transpose`를 구분해 사용합니다.

### 문제 1-1 : `reshape`로 배치 텐서 만들기

**요구 사항**

1. `A_small`의 shape과 전체 원소 수를 확인합니다.
2. `(4, 6, 특성 수)` 형태로 reshape한 `T`를 만들고 shape과 원소 수가 보존되었는지 확인합니다.
3. `-1`을 사용해 `A_small.reshape(4, -1)`을 실행하고 두 번째 축 크기가 어떻게 계산되는지 확인합니다.
4. 원소 수가 맞지 않는 reshape(예: `(5, 6, 특성 수)`)를 시도해 오류를 확인합니다.
5. reshape 전후로 첫 번째 원소(`A_small[0, 0]`과 `T[0, 0, 0]`)가 같은지 확인합니다.

- **출력 결과**
    - reshape 전후 shape과 원소 수
    - `-1` 사용 결과
    - 잘못된 reshape의 오류 메시지
    - 첫 원소 일치 여부

```bash
============================================================
[문제 1-1 : reshape로 배치 텐서 만들기]
============================================================
1. A_small shape: (24, 5), size: 120
2. T shape: (4, 6, 5), size: 120
3. reshape(4, -1, 5) -> shape: (4, 6, 5)
4. 잘못된 reshape 시도 예외: cannot reshape array of size 120 into shape (5,6,5)
5. 첫 원소 일치 여부 (A_small[0,0] == T[0,0,0]): True
```

    - reshape는 **원소 개수를 그대로 두고 축 구조만 다시 해석**하는 연산입니다. 값 자체는 하나도 바뀌지 않습니다.
    - 한 축에 `-1`을 넣으면 나머지 축 크기로부터 자동 계산됩니다. 전체 원소 수를 직접 계산하지 않아도 되어 편리합니다.
    - 전체 원소 수가 맞지 않으면 reshape는 실패합니다. 오류가 나면 목표 shape의 곱과 원본 `size`를 먼저 비교해 보면 됩니다.

### 문제 1-2 : `transpose`와 `reshape`의 차이 확인하기

**요구 사항**

1. `T`의 축 순서를 `(0, 2, 1)`로 바꾼 `T_t`를 만들고 shape을 확인합니다.
2. 같은 목표 shape을 `reshape`로도 만들어 `T_r`을 얻습니다.
3. `T_t`와 `T_r`의 값이 같은지 확인하고, 다르다면 첫 구간의 값을 직접 비교합니다.
4. `T_t`를 다시 `(0, 2, 1)`로 transpose하면 원본 `T`로 복원되는지 확인합니다.
5. transpose와 reshape의 차이를 한 문장으로 정리합니다.

- **출력 결과**
    - `T_t`, `T_r`의 shape
    - 두 결과의 값 일치 여부와 실제 값 비교
    - transpose 복원 결과
    - 두 연산의 차이 설명

```bash
============================================================
[문제 1-2 : transpose와 reshape의 차이 확인하기]
============================================================
1. T_t shape (transpose): (4, 5, 6)
2. T_r shape (reshape)  : (4, 5, 6)
3. T_t와 T_r 값 일치 여부: False
   T_t[0, 0] : [298.1 298.2 298.1 298.2 298.2 298.1]
   T_r[0, 0] : [ 298.1  308.6 1551.    42.8    0.   298.2]
4. 복원 확인 (transpose 한번 더 적용): True
5. 정리: transpose는 축의 원래 의미와 원소 관계를 유지하며 순서만 바꾸지만, reshape는 원소를 순서대로 재배치합니다.
```

    - shape이 같아도 **값이 놓인 위치는 다릅니다.** transpose는 축의 의미를 유지한 채 순서만 바꾸고, reshape는 원소를 순서대로 다시 채워 넣습니다.
    - `T_t[0, 0]`은 "첫 구간에서 첫 번째 센서의 6개 시점 값"이지만, `T_r[0, 0]`은 원소를 앞에서부터 6개 잘라 온 값이라 의미가 다릅니다.
    - 축의 의미를 바꾸고 싶을 때는 반드시 transpose를 써야 하며, 같은 축 순서를 한 번 더 적용하면 원본으로 되돌아갑니다.

---

## 필수 2 : 축을 끼워 넣고, 크기가 다른 배열끼리 계산하기

모델 입력은 항상 batch 축을 요구하는데, 샘플 하나만 추론할 때는 batch 축이 없어 오류가 납니다. 또 센서별 평균·표준편차 벡터를 전체 데이터에 적용할 때도 shape이 다릅니다. `expand_dims`/`squeeze`와 broadcasting으로 이런 상황을 해결합니다.

### 문제 2-1 : `expand_dims`와 `squeeze`로 축 조작하기

**요구 사항**

1. `(6, 5)` 크기의 샘플 하나 `sample = T[0]`을 준비하고 shape을 확인합니다.
2. `np.expand_dims(sample, axis=0)`으로 batch 축을 앞에 추가하고 shape을 확인합니다.
3. `axis=1`, `axis=-1`로도 축을 추가해 shape이 어떻게 달라지는지 비교합니다.
4. `np.squeeze`로 크기 1인 축을 제거해 원래 shape으로 돌아오는지 확인합니다.
5. 크기가 1이 아닌 축에 `squeeze`를 시도하면 어떻게 되는지 확인합니다.
6. 값의 개수는 그대로인데 왜 축 개수를 맞춰야 하는지 한 문장으로 설명합니다.

- **출력 결과**
    - 축 추가 위치별 shape 비교
    - squeeze 결과와 원본 일치 여부
    - 크기가 1이 아닌 축에 squeeze 시도 결과
    - 축 개수를 맞춰야 하는 이유

```bash
============================================================
[문제 2-1 : expand_dims와 squeeze로 축 조작하기]
============================================================
1. 원본 sample shape: (6, 5)
   expand_dims(axis= 0) -> shape: (1, 6, 5)
   expand_dims(axis= 1) -> shape: (6, 1, 5)
   expand_dims(axis=-1) -> shape: (6, 5, 1)
4. squeeze(axis=0) 후 shape: (6, 5), 원본 일치: True
5. 크기가 1이 아닌 축 squeeze 실패 예외: cannot select an axis to squeeze out which has size not equal to one
6. 이유: 축 개수(ndim)가 다르면 프레임워크에서 다른 구조의 텐서로 인식하여 차원 불일치 에러를 발생시킵니다.
```

    - `expand_dims`는 지정한 위치에 **크기 1인 축을 새로 끼워 넣습니다.** 원소 수는 그대로이고 축 개수만 늘어납니다.
    - `squeeze`는 반대로 크기 1인 축을 제거합니다. 크기가 1이 아닌 축은 제거할 수 없어 오류가 납니다.
    - 값의 개수가 같아도 **축 개수가 다르면 다른 shape으로 인식**되어, 특정 shape을 요구하는 함수나 연산에서 오류가 납니다. 그래서 모델 입력 형태에 맞춰 축 개수를 정확히 맞춰야 합니다.

### 문제 2-2 : broadcasting으로 센서별 표준화 직접 구현하기

**요구 사항**

1. `A_small`의 센서별 평균 `mean`과 표준편차 `std`를 계산하고 shape을 확인합니다. 표준편차는 `A_small.std(axis=0)`, 즉 **`np.std`(ddof=0)** 기준으로 계산합니다.
2. `(A_small - mean) / std`를 계산하고 결과 shape을 확인합니다. (broadcasting)
3. 표준화 후 각 센서의 평균이 0, 표준편차가 1에 가까운지 `np.std`로 확인합니다. (같은 결과를 pandas `.std()`로 확인하면 n=24, ddof=1 때문에 1이 아닌 1.0215가 나옵니다. 반대로 pandas 표준편차로 나눈 뒤 `np.std`로 확인하면 0.9789가 나옵니다.)
4. `mean`을 `(1, 5)`로 명시적으로 reshape한 경우와 결과가 같은지 확인합니다.
5. 잘못된 방향(예: `(24, 1)` 형태의 행 평균)으로 빼면 어떤 결과가 나오는지 확인합니다. 단, `A_small.mean(axis=1)`은 `(24,)`이므로 그대로 빼면 broadcasting 오류가 납니다. `keepdims=True`를 지정해야 `(24, 1)` 형태가 되므로, 순서대로 ① `keepdims` 없이 빼서 오류를 확인 → ② `keepdims=True`로 `(24, 1)`을 만든 뒤 결과를 비교합니다.
6. broadcasting이 어떤 규칙으로 축을 맞추는지 정리합니다.

- **출력 결과**
    - `mean` / `std`의 shape
    - 표준화 결과 shape과 검증
    - 명시적 reshape 결과와의 비교
    - 잘못된 방향 적용 결과
    - broadcasting 규칙 정리

```bash
============================================================
[문제 2-2 : broadcasting으로 센서별 표준화 직접 구현하기]
============================================================
1. A_small: (24, 5), mean: (5,), std: (5,)
2. A_scaled shape: (24, 5)
3. 센서별 평균(0에 수렴): [ 0. -0.  0. -0. -0.]
   센서별 표준편차(1에 수렴): [1. 1. 1. 1. 1.]
4. 명시적 (1, 5) 사용 결과와 동일 여부: True
5-1. keepdims 없이 axis=1 연산 시 오류: operands could not be broadcast together with shapes (24,5) (24,) 
5-2. keepdims=True (24, 1) 연산 결과 shape: (24, 5)
     잘못 적용 후 센서별 평균: [-144.678 -134.195 1094.409]
6. broadcasting 규칙: 뒤쪽 축부터 비교하여 크기가 같거나 한쪽 축 크기가 1이면 자동으로 축을 늘려서 연산합니다.
```


## 심화 1 : 행렬곱 결과 shape을 실행 전에 추론하기

모델을 조립할 때는 각 층을 통과한 뒤 shape이 어떻게 변할지 미리 알아야 다음 층을 설계할 수 있습니다. 단일 행렬곱, 배치 행렬곱, broadcasting 행렬곱 세 가지 규칙을 익히면 대부분의 딥러닝 코드를 실행 없이 읽어 낼 수 있습니다.

### 문제 3-1 : 세 가지 행렬곱의 결과 shape 추론하기

**요구 사항**

1. 다음 세 조합에 대해 **결과 shape을 먼저 예측**해 적습니다.
    - 단일 행렬곱: `(10, 512) @ (512, 256)`
    - 배치 행렬곱: `(32, 10, 512) @ (32, 512, 256)`
    - broadcasting 행렬곱: `(32, 10, 512) @ (512, 256)`
2. 각 조합을 실제로 계산해 예측과 맞는지 확인합니다.
3. 4차원 배치 행렬곱 `(32, 8, 10, 64) @ (32, 8, 64, 10)`의 결과 shape도 예측하고 확인합니다.
4. 안쪽 차원이 맞지 않는 조합 `(32, 10, 512) @ (256, 512)`를 시도해 오류를 확인합니다.
5. 결과를 표로 정리하고, 배치 축과 마지막 두 축이 각각 어떻게 처리되는지 규칙을 작성합니다.

- **출력 결과**
    - 세 조합의 예측 shape과 실제 결과
    - 4차원 배치 행렬곱 결과
    - 오류 케이스의 메시지
    - 규칙 정리 표

```bash
============================================================
[문제 3-1 : 세 가지 행렬곱의 결과 shape 추론하기]
============================================================
              연산                          입력 shape              예측              실제
          단일 행렬곱            (10, 512) @ (512, 256)       (10, 256)       (10, 256)
          배치 행렬곱    (32, 10, 512) @ (32, 512, 256)   (32, 10, 256)   (32, 10, 256)
broadcasting 행렬곱        (32, 10, 512) @ (512, 256)   (32, 10, 256)   (32, 10, 256)
      4차원 배치 행렬곱 (32, 8, 10, 64) @ (32, 8, 64, 10) (32, 8, 10, 10) (32, 8, 10, 10)

오류 케이스 확인:
안쪽 차원 불일치 예외: matmul: Input operand 1 has a mismatch in its core dimension 0, with gufunc signature (n?,k),(k,m?)->(n?,m?) (size 256 is different from 512)
```
    
    | 연산                  | 입력 shape                       | 결과 shape         |
    | 단일 행렬곱            | `(m, n) @ (n, p)`               | `(m, p)`           |
    | 배치 행렬곱            | `(batch, m, n) @ (batch, n, p)` | `(batch, m, p)`    |
    | broadcasting 행렬곱   | `(batch, m, n) @ (n, p)`         | `(batch, m, p)`    |
    - 행렬곱은 **마지막 두 축에만** 적용됩니다. 앞쪽 축(batch, head 등)은 그대로 유지되거나 broadcasting으로 맞춰집니다.
    - 확인해야 할 조건은 하나뿐입니다. 앞 텐서의 **마지막 축**과 뒤 텐서의 **끝에서 두 번째 축**이 같아야 합니다.
    - 4차원 배치 행렬곱 `(32, 8, 10, 64) @ (32, 8, 64, 10) → (32, 8, 10, 10)`은 6장에서 다룰 멀티헤드 어텐션의 score 계산과 정확히 같은 형태입니다.