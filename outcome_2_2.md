## ** [2장 2강] - 실습: 벡터공간·선형독립·Rank ** ##

## 필수 1 : 어떤 측정값을 다른 측정값들로 만들어 낼 수 있을까

설비 관리팀이 센서를 추가로 달지 검토하고 있습니다. 새 센서가 기존 센서 값들의 조합으로 설명된다면 추가 비용을 들일 이유가 없습니다. 이 판단의 수학적 도구가 **선형결합**과 **span**입니다. 작은 벡터 예제로 개념을 먼저 잡고, 다음 단계에서 실제 데이터에 적용합니다.

### 문제 1-1 : 목표 벡터를 선형결합으로 표현하기

**요구 사항**

1. 기준 벡터 `v1 = [1, 1]`, `v2 = [1, -1]`을 정의합니다. (표준기저 `[1,0]`, `[0,1]`과 달리 계수가 바로 보이지 않으니, 직접 계산해야 합니다.)
2. 목표 벡터 `target = [3, 5]`를 `c1·v1 + c2·v2`로 표현하는 계수 `c1, c2`를 `np.linalg.lstsq`로 구합니다.
3. 구한 계수로 실제 목표 벡터가 복원되는지 확인합니다.
4. `v1`, `v2`가 만드는 span이 무엇인지 한 문장으로 설명합니다.

- **출력 결과**
    - 선형결합 계수 `c1, c2`
    - 복원된 벡터와 목표 벡터의 일치 여부
    - span에 대한 설명

``` bash
=== [2장-2강] 문제 1-1 출력 결과 ===
--- [1] 선형결합 계수 계산 ---
구한 계수 c1, c2 : c1 = 4.0000, c2 = -1.0000

--- [2] 목표 벡터 복원 검증 ---
복원된 벡터          : [3. 5.]
원래 목표 벡터       : [3. 5.]
복원 벡터 일치 여부  : True

=== v1, v2가 만드는 Span의 의미 ===
두 벡터 v1=[1, 1]과 v2=[1, -1]은 서로 평행하지 않은 선형독립 벡터이므로, 이 둘의 모든 선형결합으로 생성되는 Span은 2차원 실수 좌표평면 전체(R²)가 됩니다.
```

- 여러 벡터에 스칼라를 곱해 더한 것이 **선형결합**이고, 만들 수 있는 모든 선형결합의 집합이 **span**입니다.
- 계수는 `c1 = 4`, `c2 = -1`이 나옵니다. 표준기저 `[1,0]`, `[0,1]`을 썼다면 계수가 그대로 목표 벡터의 성분 `(3, 5)`이 되어 질문이 자명해집니다. 기저를 바꿔야 **계수를 실제로 구하는 계산**이 의미를 갖습니다.
- `v1`, `v2`는 서로 평행하지 않으므로 두 벡터의 span은 **2차원 평면 전체**입니다. 표준기저가 아니어도 서로 독립인 벡터 2개만 있으면 평면 위의 어떤 벡터든 만들 수 있습니다.

### 문제 1-2 : 평행한 벡터들의 span 확인하기

**요구 사항**

1. 서로 평행한 두 벡터 `w1 = [1, 2]`, `w2 = [2, 4]`(= `2 * w1`)를 정의합니다.
2. 두 벡터를 열로 묶은 행렬의 rank를 계산합니다.
3. 목표 벡터 `[3, 5]`를 이 두 벡터의 선형결합으로 표현해 보고, 복원 결과가 목표와 일치하는지 확인합니다.
4. 목표 벡터 `[2, 4]`(= `w1`의 배수)로 바꿔 다시 시도하고 결과를 비교합니다.
5. 평행한 두 벡터의 span이 왜 평면이 아닌지 한 문장으로 설명합니다.

- **출력 결과**
    - 두 벡터로 만든 행렬의 rank
    - `[3, 5]` 복원 시도 결과와 오차
    - `[2, 4]` 복원 시도 결과
    - span에 대한 설명

```bash
=== [2장-2강] 문제 1-2 출력 결과 ===
--- [1] 평행한 벡터 행렬의 Rank ---
행렬 W의 Rank : 1

--- [2] target = [3, 5] (Span 외부 점) 복원 시도 ---
추정 계수 (c1, c2)  : [0.52 1.04]
복원된 벡터         : [2.6 5.2]
목표와 일치 여부    : False
복원 오차 (L2 norm) : 0.4472

--- [3] target = [2, 4] (Span 내부 점) 복원 시도 ---
추정 계수 (c1, c2)  : [0.4 0.8]
복원된 벡터         : [2. 4.]
목표와 일치 여부    : True
복원 오차 (L2 norm) : 0.0000

=== 평행한 두 벡터의 Span이 평면이 아닌 이유 ===
두 벡터 w1과 w2는 동일한 방향(y = 2x 직선)을 가리키는 선형종속 관계이므로, 이들의 모든 선형결합은 2차원 평면 전체를 채우지 못하고 원점을 지나는 1차원 직선에 갇히게 됩니다.
```

- `w2`가 `w1`의 스칼라배이므로 두 벡터는 **선형종속**이고, 행렬의 rank는 2가 아니라 1입니다.
- 두 벡터의 span은 평면이 아니라 `w1` 방향의 **직선 하나**입니다. 그래서 그 직선 위에 없는 `[3, 5]`는 오차 없이 복원되지 않고, 직선 위에 있는 `[2, 4]`는 정확히 복원됩니다.
- 벡터를 아무리 많이 모아도 서로 종속이면 표현 가능한 공간은 넓어지지 않습니다. 이것이 새 센서 추가 여부를 판단하는 기준이 됩니다.


### 문제 2-1 : 데이터 행렬의 rank 계산하기

**요구 사항**

1. `A`의 shape과 `np.linalg.matrix_rank(A)`를 계산합니다.
2. 가능한 최대 rank(`min(A.shape)`)와 비교합니다.
3. 컬럼 간 상관계수 행렬을 출력하고, 상관계수의 절댓값이 가장 큰 컬럼 쌍 두 개를 찾아 적습니다. (공기 온도-공정 온도 ≈ 0.876, 회전 속도-토크 ≈ -0.875)
4. 상관계수가 0.876이나 되는 컬럼 쌍이 있는데도 rank가 컬럼 수와 동일하게 5로 나오는 이유를 설명합니다. 그리고 두 컬럼이 **정확하게 선형종속**이 되어 rank가 실제로 깎이려면 상관계수가 얼마여야 하는지 답합니다. (이 답은 바로 다음 문제 2-2에서 상관 1.0인 컬럼을 직접 만들어 확인합니다.)

- **출력 결과**
    - 데이터 행렬 shape과 rank
    - 최대 가능 rank
    - 컬럼 간 상관계수 행렬
    - 데이터 상태 해석

```bash
=== [2장-2강] 문제 2-1 출력 결과 ===
--- [1] 데이터 행렬 A의 차원 및 Rank ---
행렬 A의 Shape    : (10000, 5) (샘플 수: 10000, 특성 수: 5)
행렬 A의 Rank     : 5
최대 가능 Rank    : 5
Full Rank 여부    : True (열의 개수 5개와 동일)

--- [2] 컬럼 간 상관계수 행렬 (Correlation Matrix) ---
                     Air temperature  ...  Tool wear
Air temperature               1.0000  ...     0.0139
Process temperature           0.8761  ...     0.0135
Rotational speed              0.0227  ...     0.0002
Torque                       -0.0138  ...    -0.0031
Tool wear                     0.0139  ...     1.0000

[5 rows x 5 columns]

--- [3] 절댓값 기준 상위 상관계수 컬럼 쌍 ---
• Air temperature - Process temperature : 0.8761 (절댓값: 0.8761)
• Rotational speed - Torque : -0.8750 (절댓값: 0.8750)

=== [4] 데이터 상태 해석 및 선형독립성 논리 ===
1) 높은 상관계수에도 Rank가 5(Full Rank)인 이유:
   상관계수(0.876)가 매우 높다는 것은 두 센서 변수가 '강한 경향성'을 함께 보인다는 의미일 뿐,    한 컬럼이 다른 컬럼의 '정확한 상수배(c · v)'로 표현된다는 의미가 아닙니다.    미세한 노이즈나 독자적인 정보가 존재하는 한 수학적으로는 완벽한 선형독립 상태를 유지하므로 Rank 손실이 발생하지 않습니다.
2) Rank가 실제로 깎이기 위해 필요한 상관계수:
   한 컬럼이 다른 컬럼과 '정확하게 선형종속'이 되어 Rank가 감소하려면,    두 변수 간의 상관계수가 정확히 1.0 또는 -1.0이어야 합니다.
```

- rank는 **열공간의 차원**, 즉 서로 독립적인 컬럼이 몇 개인지를 알려 줍니다.
- rank가 컬럼 수와 같다면 모든 센서가 서로 다른 정보를 담고 있다는 뜻이고, 작다면 일부 컬럼이 다른 컬럼의 조합으로 설명된다는 뜻입니다.
- 이 데이터에서는 공기 온도-공정 온도의 상관이 **0.876**이나 되지만, rank는 컬럼 수와 동일하게 **5(풀랭크)**입니다. 상관이 높은 것은 두 컬럼이 "대충 같은 방향을 향한다"는 뜻이지, 한 컬럼이 다른 컬럼으로 **정확하게** 계산된다는 뜻은 아니기 때문입니다.
- rank가 실제로 깎이려면 상관계수가 **정확하게 1.0(또는 -1.0)**이어야 합니다. 오차가 조금이라도 남아 있으면 그 컬럼은 기존 컬럼으로 설명되지 않는 방향을 조금이라도 갖고 있어 새로운 차원을 기여합니다. 이것이 바로 다음 문제 2-2에서 `A[:, 0] * 2`처럼 상관이 정확히 1.0인 컬럼을 만들어 보는 이유입니다.
- 실무 관점에서 상관 0.876은 rank를 깎지는 않지만 **다중공선성**이라는 다른 문제를 만듭니다. 역행렬은 존재하지만 조건수가 커져서 회귀계수가 불안정해집니다. "rank는 안 깎임 = 문제 없음"이 아니니 구분해서 보아야 합니다.

### 문제 2-2 : 선형종속 컬럼을 추가해도 rank가 늘지 않음 확인하기

**요구 사항**

1. 기존 행렬 `A`에 두 개의 컬럼을 추가한 `A_dep`을 만듭니다.
    - `A[:, 0] * 2` (첫 컬럼의 배수)
    - `A[:, 1] + A[:, 2]` (두 컬럼의 합)
2. `A`와 `A_dep`의 shape과 rank를 각각 계산해 비교합니다.
3. 추가로 **난수 컬럼** 하나를 붙인 `A_rand`의 rank도 계산해 비교합니다.
4. "컬럼이 늘어도 rank가 늘지 않는" 경우와 "늘어나는" 경우의 차이를 한 문장으로 설명합니다.

- **출력 결과**
    - `A` / `A_dep` / `A_rand`의 shape과 rank 비교 표
    - 컬럼 추가와 rank 변화의 관계 설명

```bash
=== [2장-2강] 문제 2-2 출력 결과 ===
--- [1] A / A_dep / A_rand의 Shape 및 Rank 비교 표 ---
행렬 종류     | Shape (행, 열) | Matrix Rank  | 추가된 컬럼의 성질                    
------------------------------------------------------------------------------
A (원본)    | (10000, 5)      | 5            | 기본 센서 데이터 (5개)                
A_dep      | (10000, 7)       | 5            | 선형종속 컬럼 2개 (2*col1, col2+col3)
A_rand     | (10000, 6)       | 6            | 선형독립 난수 컬럼 1개                 
------------------------------------------------------------------------------

=== [2] 컬럼 추가와 Rank 변화의 관계 설명 ===
추가된 컬럼이 기존 컬럼들의 선형결합(스칼라배, 합 등)으로 표현 가능한 선형종속 관계이면 새로운 정보나 공간의 차원을 제공하지 못하므로 Rank가 전혀 늘어나지 않지만, 기존 컬럼들로 설명할 수 없는 독자적인 차원을 가진 선형독립 컬럼(난수 등)이 추가되면 기하학적 차원이 확장되어 Rank가 1 증가합니다.
```

- 종속 컬럼을 2개 추가하면 **컬럼 수는 2 늘지만 rank는 그대로**입니다. 기존 컬럼의 선형결합이라 새로운 정보가 없기 때문입니다.
- 반면 난수 컬럼은 기존 컬럼으로 설명되지 않으므로 rank가 1 늘어납니다.
- 즉 "컬럼이 많다 = 정보가 많다"가 아니며, 정보량의 기준은 컬럼 수가 아니라 **rank**입니다.

## 심화 1 : 중복 특성이 남긴 흔적, 영공간

rank가 컬럼 수보다 작다는 것은 알았지만, 어떤 컬럼이 어떤 조합으로 중복인지까지 알면 제거 대상을 특정할 수 있습니다. `Ax = 0`을 만족하는 0이 아닌 벡터 `x`(영공간의 원소)가 바로 그 중복 관계를 알려 줍니다.

### 문제 3-1 : 영공간의 비자명한 해로 중복 관계 확인하기

**요구 사항**

1. `A_dep`에서 "첫 컬럼의 2배가 마지막에서 두 번째 컬럼"이라는 관계를 계수 벡터 `c1`으로 표현합니다.
2. `A_dep @ c1`이 0에 가까운지 확인합니다. (영공간의 원소)
3. `A[:, 1] + A[:, 2]`가 마지막 컬럼이라는 관계도 계수 벡터 `c2`로 표현하고 같은 방식으로 확인합니다.
4. `A_dep`의 컬럼 수, rank, 영공간의 차원(= 컬럼 수 − rank)을 계산해 세 값의 관계를 확인합니다.
5. 비교를 위해 `A`(중복 없음)에서는 `Ax = 0`의 해가 `x = 0`뿐인지 rank로 확인합니다.
6. 영공간의 차원이 0보다 클 때 데이터·모델 관점에서 어떤 의미인지 2~3문장으로 작성합니다.

- **출력 결과**
    - `A_dep @ c1`, `A_dep @ c2`의 노름
    - `A_dep`의 컬럼 수 / rank / 영공간 차원
    - `A`의 영공간 차원
    - 중복 특성에 대한 해석

```bash
=== [2장-2강] 심화 1 (문제 3-1) 출력 결과 ===
--- [1] 영공간 계수 벡터 검증 (Ax = 0) ---
c1 벡터 (2*col0 - col5 = 0)    : [ 2.  0.  0.  0.  0. -1.  0.]
||A_dep @ c1|| (L2 Norm)        : 0.000000e+00
c2 벡터 (col1 + col2 - col6 = 0) : [ 0.  1.  1.  0.  0.  0. -1.]
||A_dep @ c2|| (L2 Norm)        : 0.000000e+00

--- [2] 행렬 차원 및 영공간(Nullspace) 차원 비교 ---
• A_dep : 컬럼 수 = 7 | Rank = 5 | 영공간 차원(Nullity) = 2
• A     : 컬럼 수 = 5 | Rank = 5 | 영공간 차원(Nullity) = 0

=== [3] 중복 특성에 대한 데이터·모델 관점의 해석 ===
영공간의 차원(Nullity)이 0보다 크다는 것은 데이터에 다른 특성들의 선형결합으로 완벽히 설명되는 'redundant(중복)' 특성이 존재함을 뜻합니다. 이 경우 선형 회귀 모델에서 $(A^T A)^{-1}$ 역행렬 계산 시 유일해를 구하지 못하거나 계수(Weight)가 폭주하는 다중공선성 문제가 발생합니다. 따라서 영공간을 형성하는 영벡터 $x 
eq 0$를 추적함으로써 완전히 중복된 센서 특성을 찾아 제거(Feature Selection)하거나 차원을 축소해야 합니다.
```

- `A_dep @ c ≈ 0`인 0이 아닌 벡터가 존재한다는 것은 컬럼들 사이에 **정확한 선형 관계가 있다**는 직접적인 증거입니다. 계수 벡터를 읽으면 어떤 컬럼끼리 묶여 있는지 바로 알 수 있습니다.
- `컬럼 수 = rank + 영공간 차원`이 성립합니다. 영공간 차원이 곧 "중복된 방향의 개수"입니다.
- 중복이 없는 `A`에서는 영공간 차원이 0이고, `Ax = 0`의 해는 `x = 0`뿐입니다.
- 영공간 차원이 0보다 크면 회귀에서 `XᵀX`의 역행렬이 존재하지 않아 회귀계수가 하나로 정해지지 않습니다. 실무에서는 중복 컬럼을 제거하거나 규제를 추가해 대응하며, 이 문제는 2장 3강의 "해가 무한히 많은 경우"와 직접 이어집니다.