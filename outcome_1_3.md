## 필수 1: 고객 수천 명의 반응 점수를 한 번에 계산하기

은행 마케팅팀은 텔레마케팅 대상 고객마다 "가입 가능성 점수"를 계산해 우선순위를 정하려 합니다. 고객을 한 명씩 반복문으로 계산하면 데이터가 커질수록 느려지므로, 고객 전체를 하나의 데이터 행렬로 묶어 행렬곱 한 번으로 처리해야 합니다.

문제 1-1 : 데이터 행렬 X와 완전연결층 shape 확인하기

**요구 사항**

1. `X`의 shape을 확인하고, 각 축(행/열)이 무엇을 의미하는지 설명합니다.
2. 출력 점수를 3개 만들기 위한 가중치 행렬 `W`와 편향 벡터 `b`를 난수로 생성합니다. `W`와 `b`의 shape을 **직접 정하고 그 근거를 적습니다.**
3. `Y = X @ W + b`를 계산합니다.
4. `X`, `W`, `b`, `Y`의 shape을 출력하고, `Y`의 shape이 왜 그렇게 결정되는지 설명합니다.
- **출력 결과**
    - X / W / b / Y 각각의 shape
    - 각 축의 의미 설명
    - Y shape 결정 근거

```bash
데이터 행렬 X: (8, 5)
=== [1장-3강] 문제 1-1 출력 결과 ===
X shape : (8, 5)
W shape : (5, 3)
b shape : (3,)
Y shape : (8, 3)

=== 각 축의 의미 설명 ===
- X (8, 5): 고객 수 8명(Axis 0), 입력 특성 수 5개(Axis 1)
- W (5, 3): 입력 특성 수 5개(Axis 0), 출력 점수 종류 3개(Axis 1)
- b (3,)  : 출력 점수별 기본 편향값 3개
- Y (8, 3): 고객 8명의 3개 상품별 반응 점수

=== Y Shape 결정 근거 ===
X의 열(5)과 W의 행(5)이 일치하여 행렬곱이 가능하며,
결과 행렬 Y의 Shape은 X의 행 크기(8)와 W의 열 크기(3)에 따라 (8, 3)으로 결정됩니다.
```
- `X`는 **행이 고객 한 명, 열이 특성 하나**인 데이터 행렬입니다. 여러 고객을 한 번에 계산하기 위해 이렇게 묶습니다.
- 행렬곱 `(8, 5) @ (5, 3)`은 안쪽 차원 5가 맞아떨어지므로 결과가 `(8, 3)`이 됩니다. 즉 `Y`의 각 행은 고객 한 명에 대한 3개의 출력 점수입니다.
- `b`의 shape은 `(3,)`으로 `Y`의 `(8, 3)`과 다르지만, broadcasting 규칙에 따라 모든 행에 동일하게 더해집니다.

문제 1-2 : 반복문 계산과 행렬곱 결과가 같은지 확인하기

요구 사항
for 문으로 고객 한 명씩 x @ W + b를 계산해 리스트에 담고 배열로 만듭니다.
문제 1-1에서 계산한 Y와 값이 같은지 np.allclose로 확인합니다.
두 방식의 결과 shape이 같은지 확인합니다.
배치로 묶어 계산하는 방식의 장점을 한 문장으로 작성합니다.

출력 결과
반복문 계산 결과 shape
두 결과의 일치 여부
배치 처리의 장점 설명

```bash
=== [1장-3강] 문제 1-2 출력 결과 ===
반복문 계산 결과 shape : (8, 3)
두 결과의 일치 여부 (np.allclose) : True
두 결과의 Shape 동일 여부 : True

=== 배치 처리의 장점 ===
반복문(for) 대신 전체 데이터를 행렬(Batch) 단위로 묶어 연산하면, C 언어 수준의 벡터화(Vectorization) 및 병렬 처리가 가능해져 메모리 접근 효율과 계산 속도가 획기적으로 향상됩니다.
```

- 두 방식의 결과가 완전히 같습니다. 행렬곱은 **샘플별 계산을 한 번에 묶어 수행**하는 것과 동일합니다.
- 결과가 같더라도 행렬곱 한 줄이 더 짧고, 내부적으로 최적화된 연산을 사용하므로 데이터가 커질수록 속도 차이가 커집니다.

### 문제 2-1 : shape 오류를 재현하고 수정하기

**요구 사항**

1. 입력 차원과 맞지 않는 가중치 `W_wrong`(예: `(4, 3)`)을 만듭니다.
2. `X.shape`와 `W_wrong.shape`을 나란히 출력하고, `X @ W_wrong`을 시도합니다.
3. `try/except`로 오류 메시지를 출력하고, 앞서 출력한 두 shape과 대조해 어떤 차원이 맞지 않는지 확인합니다. (numpy 버전에 따라 오류 메시지가 전체 shape을 보여주지 않을 수 있으므로, shape 출력과 함께 판단합니다.)
4. 올바른 shape의 `W_fixed`로 수정해 다시 계산합니다.
5. 행렬곱이 가능한 조건을 한 문장으로 정리합니다.

- **출력 결과**
    - `X`, `W_wrong`의 shape
    - 발생한 오류 메시지
    - 수정 후 계산 결과 shape
    - 행렬곱 가능 조건 정리

```bash
=== [1장-3강] 문제 2-1 출력 결과 ===
X.shape: (8, 5), W_wrong.shape: (4, 3)
발생한 오류 메시지 : matmul: Input operand 1 has a mismatch in its core dimension 0, with gufunc signature (n?,k),(k,m?)->(n?,m?) (size 4 is different from 5)

수정 후 계산 결과 Y_fixed shape : (8, 3)

=== 행렬곱 가능 조건 정리 ===
두 행렬 A(m, n)와 B(k, l)의 행렬곱(A @ B)이 성립하려면 앞 행렬 A의 열 개수(n)와 뒤 행렬 B의 행 개수(k)가 반드시 일치해야 합니다.
```
- 행렬곱 `A @ B`는 **A의 열 수와 B의 행 수가 같을 때만** 가능합니다. 결과의 (i, j) 원소가 A의 i번째 행과 B의 j번째 열의 내적이기 때문입니다.
- numpy 버전에 따라 오류 메시지가 전체 shape을 다 알려주지 않을 수 있으므로, 오류를 만나기 전에 관련 배열들의 shape을 직접 출력해 비교하는 습관이 실무에서도 더 안정적인 디버깅 방법입니다.

### 문제 2-2 : 전치로 shape 맞추고 전치 성질 검증하기

<aside>

**요구 사항**

1. `X.T`의 shape을 확인하고 원본과 어떻게 달라졌는지 설명합니다.
2. `(X @ W).T`와 `W.T @ X.T`를 각각 계산해 shape과 값이 같은지 확인합니다.
3. 비교용으로 `X.T @ W.T`가 계산 가능한지 확인하고, 안 된다면 그 이유를 적습니다.
4. 전치를 취할 때 곱하는 순서가 바뀌는 이유를 한 문장으로 작성합니다.
</aside>

- **출력 결과**
    - 전치 전후 shape
    - `(XW)ᵀ`와 `WᵀXᵀ`의 일치 여부
    - 순서를 유지한 경우의 결과
    - 전치 성질 설명

```bash
=== [1장-3강] 문제 2-2 출력 결과 ===
X 전치 전 shape : (8, 5)
X 전치 후 (X.T) shape : (5, 8)

(X @ W).T shape : (3, 8)
W.T @ X.T shape  : (3, 8)
두 결과의 일치 여부 (np.allclose) : True

=== 순서를 유지한 경우 (X.T @ W.T) 시도 ===
계산 가능 여부 : 불가능 (오류 발생)
발생한 오류 메시지 : matmul: Input operand 1 has a mismatch in its core dimension 0, with gufunc signature (n?,k),(k,m?)->(n?,m?) (size 3 is different from 8)

=== 전치 성질 설명 ===
행렬곱이 성립하려면 앞 행렬의 열과 뒤 행렬의 행 크기가 같아야 하므로, 각 행렬을 전치하면 차원의 축이 뒤바뀌어 연산 순서도 반대로 전환됩니다.
```
- 전치는 행과 열을 맞바꾸는 연산이므로 `(8, 5)`가 `(5, 8)`이 됩니다.
- `(AB)ᵀ = BᵀAᵀ`가 성립합니다. 전치하면 각 행렬의 행·열이 뒤집히기 때문에, **곱하는 순서까지 바꿔야 안쪽 차원이 맞습니다.**
- 순서를 그대로 둔 `AᵀBᵀ`는 안쪽 차원이 맞지 않아 계산 자체가 불가능합니다.

## 심화 1 : 층을 두 개 쌓았을 때의 shape 흐름 추적하기

<aside>

실제 모델은 완전연결층 하나로 끝나지 않고 여러 층을 이어 붙입니다. 층을 쌓을 때마다 출력 차원이 다음 층의 입력 차원이 되므로, shape 흐름을 정확히 추적하지 못하면 모델을 조립할 수 없습니다. 은닉층을 하나 추가해 데이터가 어떤 shape으로 흘러가는지 확인합니다.


### 문제 3-1 : 2층 구조의 shape 흐름과 파라미터 수 계산하기


**요구 사항**

1. 입력 차원 5 → 은닉 차원 4 → 출력 차원 2가 되도록 `W1, b1, W2, b2`를 생성합니다.
2. `H = X @ W1 + b1`, `O = H @ W2 + b2`를 계산합니다.
3. `X → H → O`의 shape 변화를 순서대로 출력합니다.
4. 각 층의 파라미터 개수(W의 원소 수 + b의 원소 수)와 전체 파라미터 수를 계산합니다.
5. 은닉 차원을 4에서 16으로 바꾸면 출력 shape과 파라미터 수가 어떻게 변하는지 예측한 뒤 실행해 확인합니다.

- **출력 결과**
    - X → H → O shape 흐름
    - 층별 / 전체 파라미터 수
    - 은닉 차원 변경 시의 변화

```bash
=== [1장-3강] 심화 문제 3-1 출력 결과 ===
1. Shape 흐름 (은닉 차원 = 4)
   X shape : (8, 5)
   H shape : (8, 4)
   O shape : (8, 2)

2. 파라미터 수 계산 (은닉 차원 = 4)
   1층 파라미터 수 (W1 + b1) : 20 + 4 = 24
   2층 파라미터 수 (W2 + b2) : 8 + 2 = 10
   전체 파라미터 수          : 34개

3. 은닉 차원 변경 시 (4 -> 16)
   변화된 H_new shape : (8, 16)
   최종 O_new shape   : (8, 2) (최종 출력 Shape은 동일)
   1층 파라미터 수    : 80 + 16 = 96
   2층 파라미터 수    : 32 + 2 = 34
   새 전체 파라미터 수: 130개
```
- 샘플 수(8)는 층을 지나도 **그대로 유지**되고, 마지막 축(특성 차원)만 5 → 4 → 2로 바뀝니다. 각 행이 여전히 고객 한 명에 대응하기 때문입니다.
- 은닉 차원을 키우면 **출력 shape은 그대로**지만 파라미터 수는 크게 늘어납니다. 모델 용량과 계산 비용을 조절하는 지점이 바로 이 은닉 차원입니다.
- 층을 이어 붙이려면 앞 층의 출력 차원과 뒤 층의 입력 차원이 반드시 같아야 하며, 이것이 shape을 정확히 읽어야 하는 이유입니다.

