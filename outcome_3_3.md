## ** [3장 3강] - 실습: 직교성과 최소제곱법 ** ##

## 필수 1 : 서로 겹치지 않는 방향, 직교

품질 분석에서 "두 지표가 서로 독립적인 정보를 담고 있는가"는 중요한 질문입니다. 벡터로 보면 이 질문은 "두 벡터가 직교하는가"와 같습니다. 직교의 판별 기준과, 직교하는 축들로만 이루어진 직교행렬의 성질을 확인합니다.

### 문제 1-1 : 내적으로 직교 여부 판별하기

**요구 사항**

1. `a = [1, 0]`, `b = [0, 1]`의 내적을 계산해 직교 여부를 확인합니다.
2. `c = [1, 2]`, `d = [2, -1]`의 내적도 계산해 직교 여부를 확인합니다.
3. `e = [1, 2]`, `f = [2, 3]`처럼 직교하지 않는 쌍의 내적과, 두 벡터가 이루는 각도를 코사인 유사도로 계산합니다.
4. 세 쌍의 결과를 표로 정리합니다.
5. 직교하는 벡터들이 "정보가 겹치지 않는다"는 뜻인 이유를 한 문장으로 설명합니다.

- **출력 결과**
    - 세 벡터 쌍의 내적과 직교 여부
    - 직교하지 않는 쌍의 각도(도)
    - 직교의 의미 설명

```bash
============================================================
[문제 1-1] 내적으로 직교 여부 판별하기
============================================================
  벡터 쌍  내적 값  직교 여부
(a, b)     0   True
(c, d)     0   True
(e, f)     8  False

• 직교하지 않는 쌍 (e, f)의 사잇각: 7.13도

• 직교의 의미: 직교하는 벡터들은 성분 간 상관성이 0이므로 한 벡터가 담고 있는 정보가 다른 벡터에 전혀 겹치지 않습니다.
```
- 두 벡터의 내적이 0이면 직교하며, 기하학적으로 **두 벡터가 이루는 각도가 90도**라는 뜻입니다.
- 직교하는 벡터들은 서로 완전히 다른 방향을 가리키므로, 한 벡터가 담고 있는 정보가 다른 벡터에 전혀 겹치지 않습니다. 그래서 축으로 삼기에 가장 좋은 조합입니다.

### 문제 1-2 : 직교행렬 `QᵀQ = I` 확인하기

**요구 사항**

1. `np.linalg.qr(Xd)`로 QR 분해를 수행해 `Q`, `R`을 구하고 shape을 확인합니다.
2. `Q.T @ Q`가 단위행렬인지 확인합니다.
3. `Q`의 열들이 길이 1인지, 서로 직교하는지 각각 확인합니다.
4. 임의의 벡터 `v`에 대해 `Q.T @ (Q @ v)`가 `v`와 같은지 확인해 **`Qᵀ`가 `Q`의 왼쪽 역행렬(left inverse) 역할을 한다**는 사실을 확인합니다. 추가로 `Q @ Q.T`는 단위행렬이 **아니라는** 것도 함께 확인합니다.
5. 역행렬을 따로 구하지 않아도 되는 직교행렬의 장점을 한 문장으로 설명합니다.

- **출력 결과**
    - `Q`, `R`의 shape
    - `QᵀQ` 검증 결과
    - 열 벡터의 길이와 직교성
    - `Qᵀ`가 왼쪽 역행렬 역할을 하는지 검증(`Qᵀ(Qv) = v`), 그리고 `QQᵀ ≠ I` 확인

```bash
============================================================
[문제 1-2] 직교행렬 QᵀQ = I 확인하기                          
============================================================
1. Q shape: (6497, 6), R shape: (6, 6)              
2. QᵀQ가 단위행렬(I)과의 최대 오차: 9.44e-15 (≈ 0, QᵀQ = I 성립)
3. Q의 열 벡터 길이 (모두 1): [1. 1. 1. 1. 1. 1.]
4. Qᵀ(Qv)와 v의 차이 노름: 1.89e-15 (Qᵀ가 왼쪽 역행렬 역할 수행)
   QQᵀ가 단위행렬(I)인지 확인: False (직사각 행렬이므로 단위행렬이 아님)

5. 직교행렬의 장점: 역행렬을 복잡한 연산으로 따로 구하지 않고 전치(Qᵀ)만으로 원래 좌표를 되돌릴 수 있어 계산이 빠르고 수치적으로 안정적입니다.
```

- `Q`의 모든 열은 길이가 1이면서 서로 직교합니다(정규직교). 그래서 `QᵀQ = I`가 성립합니다.
- 단, 이번 `Q`는 샘플 수(행)가 컬럼 수보다 훨씬 많은 **직사각 행렬(thin QR)** 이므로 엄밀하게는 `Q⁻¹ = Qᵀ`라고 쓰면 안 됩니다. 정사각이 아닌 행렬에는 역행렬 자체가 없기 때문이고, 실제로 `Q @ Q.T`는 단위행렬이 아닙니다.
- 정확한 표현은 **`Qᵀ`가 `Q`의 왼쪽 역행렬(left inverse) 역할을 한다**는 것입니다(`QᵀQ = I`). 어느 쪽이든, **역행렬을 따로 계산하지 않고 전치만으로 원래 좌표를 되돌릴 수 있다는 장점은 그대로입니다.** 그래서 계산이 빠르고 수치적으로도 안정적입니다. (`Q`가 정사각이라면 그때는 진짜로 `Q⁻¹ = Qᵀ`가 됩니다.)
- 2장 1강의 회전 행렬도 직교행렬이었습니다. 직교행렬 변환은 길이와 각도를 보존한다는 공통 성질을 가집니다.

## 필수 2 : 정확한 해가 없는 회귀 문제를 투영으로 풀기

품질 예측 회귀에서는 샘플 수가 특성 수보다 훨씬 많아 모든 샘플을 정확히 맞추는 계수가 존재하지 않습니다. 이때 최소제곱법은 "오차 제곱합이 가장 작은" 근사해를 찾아 주며, 그 기하학적 의미는 **타깃 벡터를 설계행렬의 열공간에 수직으로 투영하는 것**입니다. 공식으로 직접 계산하고 그 의미를 확인합니다.

### 문제 2-1 : 정규방정식으로 회귀계수 계산하기

**요구 사항**

1. `x̂ = (XᵀX)⁻¹Xᵀy` 공식을 그대로 코드로 옮겨 계수 `coef_formula`를 구합니다.
2. `np.linalg.lstsq`로 구한 `coef_lstsq`와 값을 비교합니다.
3. `sklearn`의 `LinearRegression`으로 학습해 절편·계수를 얻고, 앞의 두 결과와 비교합니다.
    - ⚠️ `Xd`는 **이미 절편 컬럼(1로 채운 열)을 포함**하고 있습니다. 이 상태로 `LinearRegression().fit(Xd, y)`를 그냥 호출하면 sklearn이 기본값 `fit_intercept=True`로 절편을 **따로** 추정하므로, 상수항 컬럼에 해당하는 `coef_[0]`이 `0.0`이 되어 버립니다. 그러면 정규방정식 결과와 서로 어긋나 "세 방식이 일치한다"는 핵심 검증이 깨집니다.
    - 따라서 다음 둘 중 하나로 조건을 맞춰야 합니다. ① 절편 컬럼이 없는 `Xs`로 학습한 뒤 `np.r_[lr.intercept_, lr.coef_]`로 결합해 비교하거나, ② `LinearRegression(fit_intercept=False).fit(Xd, y)`로 학습해 `lr.coef_`를 그대로 비교합니다. 세 방식이 모두 일치하는지 보기 전에 이 조건을 반드시 확인하세요.
4. 세 방식의 예측 RMSE를 계산해 표로 정리합니다.
5. 세 결과가 (거의) 같은 이유를 한 문장으로 설명합니다.

- **출력 결과**
    - 세 방식의 회귀계수
    - 계수 간 차이(노름)
    - 세 방식의 RMSE 비교 표

```bash
============================================================
[문제 2-1] 정규방정식으로 회귀계수 계산하기                                 
============================================================       
1. 세 방식의 회귀계수 비교:                  
     정규방정식    lstsq  sklearn                                       
0  5.81838  5.81838  5.81838
1 -0.00631 -0.00631 -0.00631
2 -0.19957 -0.19957 -0.19957
3  0.01827  0.01827  0.01827
4 -0.08893 -0.08893 -0.08893
5 -0.11025 -0.11025 -0.11025

2. 계수 간 차이(노름):
   • 정규방정식 vs lstsq: 2.32e-14
   • 정규방정식 vs sklearn: 2.32e-14

3. 세 방식의 RMSE 비교 표:
                      방식     RMSE
                   정규방정식 0.831973
         np.linalg.lstsq 0.831973
sklearn LinearRegression 0.831973

4. 결과가 동일한 이유: 세 방식 모두 오차 제곱합을 최소화하는 동일한 수학적 대상(최소제곱 문제)을 풀기 때문에 회귀계수와 RMSE가 사실상 동일합니다.
```

- 세 방식 모두 **같은 최소제곱 문제**를 풀기 때문에 계수와 RMSE가 사실상 동일합니다. sklearn의 `LinearRegression`도 내부적으로 최소제곱해를 구합니다.
- `x̂ = (XᵀX)⁻¹Xᵀy`는 정규방정식 `XᵀXx = Xᵀy`를 양변에 `(XᵀX)⁻¹`을 곱해 정리한 닫힌 형태 해입니다. 회귀계수를 구하는 수학적 근거가 바로 이 식입니다.

### 문제 2-2 : 잔차의 직교성으로 투영 의미 확인하기

**요구 사항**

1. 예측값 `pred = Xd @ coef_lstsq`와 잔차 `resid = y - pred`를 계산합니다.
2. `Xd.T @ resid`의 노름을 계산해 0에 가까운지 확인합니다.
3. 설계행렬의 각 컬럼과 잔차의 내적을 개별적으로 출력해 모두 0에 가까운지 확인합니다.
4. 비교를 위해, 계수를 임의로 살짝 바꾼 `coef_bad`로 잔차를 구해 `Xd.T @ resid_bad`의 노름과 오차 제곱합을 비교합니다.
5. 잔차가 열공간과 직교한다는 것이 "가장 가까운 근사"라는 의미로 이어지는 이유를 2~3문장으로 작성합니다.

- **출력 결과**
    - `Xᵀ·잔차`의 노름
    - 컬럼별 내적 값
    - 최적 계수와 임의 계수의 비교(직교성, 오차 제곱합)
    - 투영 관점의 해석

```bash
============================================================
[문제 2-2] 잔차의 직교성으로 투영 의미 확인하기                                    
============================================================
1. Xᵀ · 잔차 의 노름: 1.69e-10 (≈ 0)          
                                                             
2. 설계행렬의 컬럼별 잔차와의 내적 값:
   • 컬럼 0: -1.69e-10
   • 컬럼 1: 6.66e-13
   • 컬럼 2: -8.38e-12
   • 컬럼 3: 7.04e-12
   • 컬럼 4: 2.40e-12
   • 컬럼 5: -2.03e-12

3. 최적 계수 vs 임의 계수 비교:
          지표        최적 계수       임의 계수
  Xᵀ · 잔차 노름 1.691825e-10 3638.730514
오차 제곱합 (SSE) 4.497082e+03 6121.332140

4. 투영 관점의 해석:
설계행렬의 열공간 안에서 관측값 y에 가장 가까운 점은 수직으로 내린 투영점(Xx)입니다.
이 때 최단거리를 이루는 잔차 벡터는 열공간 내부의 모든 축과 직교하게 되며(Xᵀr = 0),
이 직교 조건이 오차 제곱합을 최소로 만드는 최적 근사해의 기하학적 성질입니다.
```

- 최소제곱해에서는 잔차가 설계행렬의 **모든 컬럼과 직교**합니다(`Xᵀr ≈ 0`). 임의 계수에서는 이 값이 0에서 크게 벗어나고 오차 제곱합도 커집니다.
- 기하학적으로 `Xx`는 설계행렬의 열공간 안에 있는 벡터이고, `y`는 대개 그 공간 밖에 있습니다. 열공간 안에서 `y`에 가장 가까운 점은 **수직으로 내린 발(투영점)** 이며, 이때 오차 벡터가 열공간과 직교합니다.
- 이 직교 조건 `Xᵀ(y − Xx) = 0`을 정리하면 정확히 정규방정식 `XᵀXx = Xᵀy`가 나옵니다. 즉 투영과 정규방정식은 같은 이야기입니다.

## 심화 1 : 축을 직교로 다시 세우기 - Gram-Schmidt

설계행렬의 컬럼들은 서로 상관이 있어 방향이 겹칩니다. 이 컬럼들을 서로 직교하고 길이가 1인 축으로 다시 세우면, 회귀 계산이 안정적으로 바뀌고 각 축의 기여를 분리해 볼 수 있습니다. 이 절차가 Gram-Schmidt 과정이며, 결과는 QR 분해의 `Q`와 같습니다.

### 문제 3-1 : Gram-Schmidt로 정규직교기저 만들기

**요구 사항**

1. `Xd[:, 1:4]`(절편 컬럼을 제외한 특성 1—3번)을 선형독립인 벡터 3개로 사용합니다.
    - 절편 컬럼이 포함된 `Xd[:, :3]`을 쓰면, 표준화된 특성은 평균이 0이어서 상수항 컬럼과 이미 직교합니다. 그러면 "직교화 전후 변화"가 거의 드러나지 않으므로, 상수항을 버리고 실제로 상관이 있는 특성 컬럼 세 개를 고릅니다.
2. Gram-Schmidt 과정을 직접 구현합니다.
    - 각 벡터에서 **앞서 만든 직교 벡터 방향 성분을 빼고**(직교화)
    - 남은 벡터를 자신의 길이로 나눕니다(정규화)
3. 결과 행렬 `U`에 대해 `UᵀU = I`인지 확인합니다.
4. `np.linalg.qr`로 얻은 `Q`의 앞 3개 열과 **절댓값 기준**으로 비교합니다.
5. 원래 컬럼들 사이의 상관계수와, 직교화 이후 축들 사이의 상관 정도를 비교합니다.
6. 정규직교기저를 쓰면 어떤 점이 편해지는지 2~3문장으로 작성합니다.

- **출력 결과**
    - `UᵀU` 검증 결과
    - QR의 `Q`와 절댓값 비교 결과
    - 직교화 전후 축 간 내적 비교
    - 정규직교기저의 장점 서술

```bash

```

- Gram-Schmidt는 **직교화(앞선 축 방향 성분 제거) → 정규화(길이 1로)** 두 단계의 반복입니다. 결과는 QR 분해의 `Q`와 부호를 제외하고 동일합니다.
- 직교화 전에는 컬럼끼리 내적이 0이 아니라 방향이 겹쳐 있었지만, 직교화 후에는 대각 성분만 1이고 나머지는 0이 됩니다.
- 정규직교기저를 쓰면 `QᵀQ = I`가 성립해 `Qᵀ`가 왼쪽 역행렬 역할을 하므로 역행렬을 따로 계산할 필요가 없고, 좌표를 구할 때도 내적 한 번이면 됩니다. 최소제곱 문제도 `Rx = Qᵀb`처럼 훨씬 안정적인 형태로 바뀝니다.