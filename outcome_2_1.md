## 필수 1 : 진단 데이터 분포를 늘리고, 뒤집고, 눌러 보기

의료 데이터 시각화 담당자는 두 진단 지표의 산점도를 보고서에 넣으려 합니다. 축 비율을 조정하거나 축을 뒤집는 등 표현 방식을 바꿔야 하는데, 이런 조정은 모두 2×2 행렬 하나를 곱하는 선형 변환으로 표현됩니다. 각 변환 행렬이 분포를 어떻게 바꾸는지 직접 확인합니다.

### 문제 1-1 : 스케일링 행렬 적용하기

**요구 사항**

1. x축은 2배, y축은 0.8배로 만드는 스케일링 행렬 `S = [[2, 0], [0, 0.8]]`를 정의합니다.
2. `apply_T(S, X2)`를 계산해 변환된 좌표를 얻습니다. (이 실습의 모든 변환은 `apply_T`로 적용해 열벡터 관점을 통일합니다.)
3. 변환 전후의 각 축 표준편차를 비교합니다.
4. `plot_pair()`로 변환 전후를 함께 그립니다.
5. 대각 원소가 각각 어떤 축을 담당하는지 한 문장으로 설명합니다.

- **출력 결과**
    - 변환 전후 축별 표준편차
    - 변환 전후 산점도
    - 대각 원소의 의미 설명

```bash
=== [2장-1강] 문제 1-1 출력 결과 ===

[행렬 X2]
[[ 1.0971 -2.0733]
 [ 1.8298 -0.3536]
 [ 1.5799  0.4562]
 ...
 [ 0.7023  2.0456]
 [ 1.8383  2.3365]
 [-1.8084  1.2218]]
[행렬 X2_scaled]
[[ 2.1941 -1.6587]
 [ 3.6596 -0.2829]
 [ 3.1598  0.3649]
 ...
 [ 1.4046  1.6365]
 [ 3.6767  1.8692]
 [-3.6168  0.9774]]

=== 축별 표준편차 변화 ===
변환 전 표준편차 (x축, y축) : [1.0000, 1.0000]
변환 후 표준편차 (x축, y축) : [2.0000, 0.8000]
표준편차 변화 비율         : [2.00배, 0.80배]

=== 대각 원소의 의미 설명 ===
스케일링 행렬의 대각 원소 S[0, 0]=2.0은 x축 방향의 확대/축소 비율을, S[1, 1]=0.8은 y축 방향의 확대/축소 비율을 독립적으로 결정합니다.

[안내] 변환 전후 비교 산점도 창을 띄웁니다.
```
![문제 1-1 scailing 전후비교 산점도](images\chapter_2_1_problem_1_1_scaling_transformation.png)

- 스케일링 행렬의 대각 원소는 각각 **x축 배율, y축 배율**을 담당합니다. 첫 축은 2배로 퍼지고 두 번째 축은 0.8배로 좁아집니다.
- 표준편차가 정확히 배율만큼 변한 것으로, 이 변환이 축 방향 크기만 조정했음을 확인할 수 있습니다.


### 문제 1-2 : 반사와 투영 행렬 적용하기

**요구 사항**

1. x축 기준 반사 행렬 `F = [[1, 0], [0, -1]]`을 적용합니다.
2. 반사 전후에서 각 점의 원점까지 거리(L2 노름)가 유지되는지 확인합니다.
3. x축 위로 누르는 투영 행렬 `P = [[1, 0], [0, 0]]`을 적용합니다.
4. 투영 결과의 두 번째 축 값이 모두 0인지 확인합니다. 이어서 **같은 변환을 한 번 더 적용해 되돌릴 수 있는지**를 봅니다. `F @ F == I`인지, `P @ P == I`인지를 각각 확인하고, 다른 두 점 `[1, 2]`와 `[1, -5]`를 투영하면 어떻게 되는지도 함께 확인합니다.
5. 반사와 투영의 차이(정보 보존 여부)를 한 문장으로 설명합니다.

- **출력 결과**
    - 반사 전후 거리 유지 여부와 산점도
    - 투영 결과의 두 번째 축 값과 되돌림 가능 여부(`F @ F`, `P @ P`)
    - 두 변환의 차이 설명

```bash
=== [2장-1강] 문제 1-2 출력 결과 ===
--- [1] 반사 행렬 (F) 검증 ---
반사 전후 원점과의 거리(L2 노름) 유지 여부 : True

--- [2] 투영 행렬 (P) 검증 ---
투영 결과의 y축 값이 모두 0인지 여부       : True

--- [3] 합성 연산 및 되돌림 가능 여부 (Self-Inverse) ---
F @ F == I 여부 (두 번 반사시 원복)            : True
P @ P == I 여부 (두 번 투영시 원복)            : False  (실제 P @ P = P)

--- [4] 서로 다른 두 점 투영 결과 비교 ---
점 [1, 2]  투영 결과 : [1. 0.]
점 [1, -5] 투영 결과 : [1. 0.]
두 투영 결과의 동일 여부 : True

=== 반사와 투영의 차이 (정보 보존 여부) ===
반사(Reflection)는 공간의 길이와 각도를 보존하며 자기 자신을 역행렬로 가져(F @ F = I) 원본 복원이 가능한 반면, 투영(Projection)은 특정 차원(y축)의 정보를 완전히 0으로 눌러 파괴하므로 서로 다른 두 점이 같은 점으로 겹치고 역행렬이 존재하지 않아 원본 복원이 불가능합니다.
```
![문제 1-2 reflection 전후비교 산점도](images\chapter_2_1_problem_1_2_reflection_transformation.png)

![문제 1-2 projection 전후비교 산점도](images\chapter_2_1_problem_1_2_projection_transformation.png)

- 반사는 y좌표의 부호만 뒤집으므로 **원점까지의 거리가 그대로 유지**됩니다. 정보 손실 없이 방향만 바뀌며, 되돌리는 것도 가능합니다.
- 투영은 y좌표를 0으로 눌러 버리므로 모든 점이 x축 한 직선 위에 놓입니다. `P @ P`는 `I`가 아니라 `P` 자신이므로, 한 번 더 적용해도 그대로 제자리에 있어 **되돌릴 방법이 없습니다.**
- 이렇게 "서로 다른 여러 점이 한 점으로 모이는" 상황이 곧 차원이 줄어든다는 의미입니다. 이것을 rank라는 숫자로 세는 방법은 2장 2강에서 배웁니다.

## 필수 2 : 회전과 합성 변환, 그리고 순서 문제


시각화 담당자가 "데이터를 45도 돌린 다음 x축 방향으로 늘려 달라"는 요청과 "늘린 다음 돌려 달라"는 요청을 각각 받았습니다. 두 요청이 같은 결과를 낼 것 같지만, 행렬 곱은 순서를 바꾸면 결과가 달라집니다. 회전 행렬을 만들고 합성 순서에 따른 차이를 확인합니다.


### 문제 2-1 : 회전 행렬 만들고 적용하기

**요구 사항**

1. 45도(라디안 변환 필요)에 대한 회전 행렬 `R = [[cosθ, -sinθ], [sinθ, cosθ]]`을 만듭니다.
2. 검증용으로 벡터 `(1, 0)`에 회전 행렬을 적용해 결과가 `(0.71, 0.71)`에 가까운지 확인합니다.
3. `X2`에 회전을 적용하고 산점도로 확인합니다.
4. 회전 전후에서 원점까지의 거리가 유지되는지 확인합니다.
5. `RᵀR`을 계산해 단위행렬이 되는지 확인하고, 그 의미를 한 문장으로 설명합니다.

- **출력 결과**
    - `(1, 0)`의 회전 결과
    - 회전 전후 산점도와 거리 유지 여부
    - `RᵀR` 결과

```bash
=== [2장-1강] 문제 2-1 출력 결과 ===
--- [1] 검증용 벡터 (1, 0) 회전 결과 ---
회전 전 벡터 v      : [1. 0.]
45도 회전 후 벡터   : [0.7071, 0.7071] (목표값: [0.7071, 0.7071])

--- [2] 거리(L2 노름) 보존 여부 검증 ---
회전 전후 원점과의 거리 유지 여부 : True

--- [3] RᵀR 연산 및 단위행렬 검증 ---
RᵀR 계산 결과 :
[[ 1. -0.]
 [-0.  1.]]
RᵀR == I (단위행렬) 성립 여부 : True

=== RᵀR = I 의 의미 설명 ===
회전 행렬 R은 전치행렬이 곧 역행렬(Rᵀ = R⁻¹) 역할을 하는 직교행렬(Orthogonal Matrix)이며, 이는 공간 변환 시 벡터 간의 각도와 원점으로부터의 길이를 완벽히 보존하는 강체 변환(Rigid Transformation)임을 의미합니다.

[안내] 45도 회전 변환 전후 비교 산점도 창을 띄웁니다.
```

![문제 2-1 회전 반환 전후비교 산점도](images\chapter_2_1_problem_2_1_rotation_transformation.png)

- 회전 행렬을 `(1, 0)`에 적용하면 `(cos45°, sin45°) ≈ (0.71, 0.71)`이 나옵니다. 벡터가 원점을 중심으로 45도 돌아간 것입니다.
- 회전은 **거리와 각도를 모두 보존**하므로 데이터의 상대적 구조가 그대로 유지됩니다. 좌표계만 돌아간 것입니다.
- `RᵀR = I`이므로 회전 행렬은 직교행렬이며, 역행렬을 따로 구하지 않고 전치만으로 되돌릴 수 있습니다. 이 성질은 3장 3강 직교행렬에서 다시 다룹니다.
- 회전 행렬 $R$은 전치행렬이 곧 역행렬($R^T = R^{-1}$)이 되는 직교행렬(Orthogonal Matrix)로, 공간의 각도와 길이를 왜곡 없이 완벽히 보존하는 등거리 변환을 나타냅니다.

### 문제 2-2 : 합성 변환의 순서 비교하기

**요구 사항**

1. "스케일링 후 회전"에 해당하는 합성 행렬 `M1 = R @ S`를 만듭니다.
2. "회전 후 스케일링"에 해당하는 합성 행렬 `M2 = S @ R`를 만듭니다.
3. 두 행렬이 같은지 확인하고, 각각을 `X2`에 적용한 결과가 다른지 확인합니다.
4. 두 결과를 산점도로 비교합니다.
5. 변환 A를 먼저, B를 나중에 적용할 때 전체 변환이 `BA`가 되는 이유를 한 문장으로 설명합니다.

- **출력 결과**
    - `M1`, `M2` 행렬과 동일 여부
    - 두 합성 변환 결과 산점도
    - 합성 순서에 대한 설명

```bash
=== [2장-1강] 문제 2-2 출력 결과 ===
--- [1] 합성 행렬 비교 ---
M1 (스케일링 후 회전, R @ S) :
[[ 1.4142 -0.5657]
 [ 1.4142  0.5657]]
M2 (회전 후 스케일링, S @ R) :
[[ 1.4142 -1.4142]
 [ 0.5657  0.5657]]
M1 == M2 행렬 동일 여부 : False

--- [2] 변환 결과 비교 ---
X2_M1 == X2_M2 변환 결과 동일 여부 : False

=== 합성 순서가 BA가 되는 이유 ===
벡터 v에 변환 A를 먼저 적용한 결과 (Av)에 변환 B를 적용하면 B(Av) = (BA)v가 되므로, 입력 벡터에 가까운 오른쪽 연산자부터 순차적으로 작용하여 전체 합성 행렬은 BA가 됩니다.

[안내] 두 합성 변환(M1 vs M2) 결과 비교 산점도 창을 띄웁니다.
```

![문제 2-2 합성 순서 비교 산점도](images\chapter_2_1_problem_2_2_composite_transformation_order.png)

- 두 합성 행렬은 서로 다르고, 적용 결과의 분포 모양도 다릅니다. **합성 변환에서는 순서가 곧 의미**입니다.
- 변환 A를 먼저, B를 나중에 적용하면 벡터 기준으로 `B(Av)`가 되므로 전체 변환은 `BA`입니다. 나중에 적용하는 변환이 왼쪽에 옵니다.
- 스케일링 후 회전은 늘어난 모양을 통째로 돌리지만, 회전 후 스케일링은 돌아간 상태에서 좌표축 방향으로 늘리기 때문에 최종 모양이 달라집니다.

## 심화 1 : 이 변환은 정말 선형 변환일까?

보고서용으로 산점도 전체를 오른쪽 위로 옮겨 달라는 요청이 들어왔습니다. 이런 평행이동은 행렬 곱만으로는 표현할 수 없고, 실제로 선형 변환의 조건을 만족하지 않습니다. 선형 변환의 정의를 코드로 검증해 무엇이 선형이고 무엇이 아닌지 구분합니다.

### 문제 3-1 : 가산성·동차성으로 선형 변환 판별하기

**요구 사항**

1. 행렬 곱만 하는 변환 `T_linear(v) = apply_T(M1, v)`와, 평행이동을 더한 `T_affine(v) = apply_T(M1, v) + bias`를 정의합니다. (`bias = [3, -2]`)
2. 임의의 두 벡터 `u`, `v`와 스칼라 `c`를 정해, 두 변환에 대해 각각 다음을 확인합니다.
    - 가산성: `T(u + v) == T(u) + T(v)`
    - 동차성: `T(c·u) == c·T(u)`
    - 원점 보존: `T(0) == 0`
3. 두 변환의 결과를 표로 정리합니다.
4. `X2`에 두 변환을 적용해 산점도로 차이를 확인합니다.
5. 평행이동이 선형 변환이 아닌 이유와, 그럼에도 `XW + b`가 딥러닝에서 널리 쓰이는 이유를 2~3문장으로 작성합니다.

- **출력 결과**
    - 두 변환의 가산성·동차성·원점 보존 검증 결과 표
    - 변환 결과 산점도
    - 선형 변환 여부에 대한 설명

```bash

